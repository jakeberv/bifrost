% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulation-studies.R
\name{evaluateShiftRecovery}
\alias{evaluateShiftRecovery}
\title{Evaluate Shift Recovery Against Simulated Ground Truth}
\usage{
evaluateShiftRecovery(
  simdata,
  simresults,
  fuzzy_distance = 2,
  weighted = TRUE,
  verbose = TRUE
)
}
\arguments{
\item{simdata}{A list of simulated shifted datasets, typically the \code{simdata}
component returned by \code{\link[=runShiftRecoverySimulationStudy]{runShiftRecoverySimulationStudy()}}. Each element must
contain at least \code{paintedTree} and \code{shiftNodes}.}

\item{simresults}{A list of search results corresponding to \code{simdata}, usually
the \code{results} component returned by \code{\link[=runShiftRecoverySimulationStudy]{runShiftRecoverySimulationStudy()}}.
Each element should contain \code{shift_nodes_no_uncertainty} and
\code{num_candidates}. Results from \code{\link[=searchOptimalConfiguration]{searchOptimalConfiguration()}} also carry
\code{candidate_nodes}, which allows candidate-aware specificity calculations;
legacy result objects without that component retain the historical
count-only calculation. If \code{weighted = TRUE}, \code{ic_weights} is also used
when available.}

\item{fuzzy_distance}{Integer node distance threshold used for fuzzy matching.}

\item{weighted}{Logical; if \code{TRUE}, compute weighted precision/recall/F1 using
the inferred-node IC weights from each search result when available.}

\item{verbose}{Logical; if \code{TRUE}, print a compact summary of the strict,
fuzzy, and weighted metrics.}
}
\value{
A list of class \code{bifrost_shift_recovery_evaluation} with five
components:
\describe{
\item{\code{strict}}{Strict precision, recall, F1, specificity, false-positive
rate, and balanced accuracy.}
\item{\code{fuzzy}}{The same metrics under fuzzy matching.}
\item{\code{weighted}}{Weighted strict and fuzzy precision/recall/F1 summaries
when \code{weighted = TRUE}; otherwise \code{NULL}.}
\item{\code{counts}}{Aggregated contingency-table counts for the strict and fuzzy
matching schemes.}
\item{\code{n_evaluable_replicates}}{Number of replicate pairs included in the
aggregated counts after failed or structurally incomplete records are
excluded.}
}
}
\description{
Compare inferred shift locations against known simulated shift locations across
multiple datasets using both strict node matching and fuzzy matching within a
configurable node distance. This is a package-clean export of the manuscript's
\code{evaluate_shift_recovery()} function.
}
\details{
The evaluation uses three complementary summaries:
\itemize{
\item strict metrics count only exact node matches,
\item fuzzy metrics allow inferred shifts within \code{fuzzy_distance} nodes of a
true shift, using a greedy one-to-one assignment, and
\item weighted metrics apply IC weights only to inferred nodes, following
the simulation-study implementation.
}

Search results carrying a non-empty \code{error} field are retained by the study
wrappers for diagnosis but excluded from recovery counts and metrics.

For current search results, true-negative counts are calculated over the
explicit \code{candidate_nodes} universe. True shifts excluded by the search's
candidate filter still contribute false negatives, but are not subtracted
from the number of candidate negatives. This prevents specificity and
balanced accuracy from being distorted when a simulated shift is not an
eligible search candidate. Supplied candidate-node vectors must contain
unique, valid whole-number node IDs and agree with \code{num_candidates}. For
compatibility, result objects that provide only \code{num_candidates} use the
historical count-only formula.

When a replicate has no evaluable candidate shifts (\code{num_candidates == 0}),
recall-style quantities can still be computed from the true and inferred
shifts, but specificity, false-positive rate, and balanced accuracy are
returned as \code{NA} because there are no evaluable negatives.
}
\examples{
\dontrun{
# Usually called on the output of runShiftRecoverySimulationStudy():
# evaluateShiftRecovery(study$simdata, study$results, fuzzy_distance = 2)
}

}
\seealso{
\code{\link[=runShiftRecoverySimulationStudy]{runShiftRecoverySimulationStudy()}}, \code{\link[=simulateShiftedDataset]{simulateShiftedDataset()}}
}
