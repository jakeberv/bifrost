% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulation-tuning.R
\name{runSearchTuningGrid}
\alias{runSearchTuningGrid}
\title{Run a Fixed-IC Search Tuning Grid}
\usage{
runSearchTuningGrid(
  template,
  IC = c("GIC", "BIC"),
  shift_acceptance_thresholds,
  min_descendant_tips_values,
  tree_tip_count = NULL,
  null_replicates = 50,
  recovery_replicates = 50,
  null_simulation_options = list(),
  proportional_simulation_options,
  correlation_simulation_options = NULL,
  base_search_options = list(),
  fuzzy_distance = 2,
  weighted = TRUE,
  num_cores = 1,
  seed = NULL,
  store_studies = FALSE
)
}
\arguments{
\item{template}{A \code{bifrost_simulation_template} returned by
\code{\link[=createSimulationTemplate]{createSimulationTemplate()}}.}

\item{IC}{Character scalar, either \code{"GIC"} or \code{"BIC"}. The tuning grid is
run for this IC family only.}

\item{shift_acceptance_thresholds}{Finite, non-negative numeric vector of
candidate \code{shift_acceptance_threshold} values to evaluate.}

\item{min_descendant_tips_values}{Integer vector of candidate
\code{min_descendant_tips} values to evaluate.}

\item{tree_tip_count}{Optional integer tip count passed to the simulation
study wrappers. If \code{NULL}, each study uses the full empirical tree.}

\item{null_replicates}{Integer number of null replicates run for each grid
row.}

\item{recovery_replicates}{Integer number of shifted replicates run for each
grid row in both the proportional and correlation scenarios.}

\item{null_simulation_options}{Named list forwarded to
\code{\link[=runFalsePositiveSimulationStudy]{runFalsePositiveSimulationStudy()}}. The \code{"original"} manuscript generator
is used when \code{simulation_generator} is omitted. Its \code{simulation_generator}
entry must contain exactly one supported string.}

\item{proportional_simulation_options}{Named list forwarded to
\code{\link[=runShiftRecoverySimulationStudy]{runShiftRecoverySimulationStudy()}} for the proportional generating-model
scenario. Must include \code{num_shifts}, \code{min_shift_tips}, and
\code{max_shift_tips}. Its \code{simulation_generator} entry must contain exactly one
supported string.}

\item{correlation_simulation_options}{Optional named list forwarded to
\code{\link[=runShiftRecoverySimulationStudy]{runShiftRecoverySimulationStudy()}} for the non-proportional robustness
scenario. The argument name is retained for compatibility with the
\code{scale_mode = "correlation"} label. Under the default \code{"original"}
generator it controls the published transform; under
\code{simulation_generator = "empirical"} it controls the integration-rate
trade-off. Its \code{simulation_generator} entry must contain exactly one
supported string. If
\code{NULL}, the proportional options are reused and only \code{scale_mode} is
changed to \code{"correlation"}.}

\item{base_search_options}{Named list of search options shared across all
grid rows. The tuning helper overwrites \code{IC}, \code{shift_acceptance_threshold},
and \code{min_descendant_tips} for each row. Simulation-grid searches are
intentionally intercept-only, so \code{formula} should remain \code{"trait_data ~ 1"}
(or an equivalent intercept-only response formula). If omitted, the helper
inherits \code{template$search_formula}.}

\item{fuzzy_distance}{Integer node distance passed to
\code{\link[=evaluateShiftRecovery]{evaluateShiftRecovery()}} inside the shifted-study wrappers.}

\item{weighted}{Logical; if \code{TRUE}, request weighted recovery summaries from
\code{\link[=runShiftRecoverySimulationStudy]{runShiftRecoverySimulationStudy()}} and include weighted fuzzy F1 columns
in the output table.}

\item{num_cores}{Integer number of workers used across grid settings. When
\code{num_cores > 1}, \code{\link[=runSearchTuningGrid]{runSearchTuningGrid()}} parallelizes over settings and
forces the dependent study wrappers and search calls to run serially within
each setting.}

\item{seed}{Optional integer seed used to derive shared per-scenario study
seeds. All grid rows use paired simulated datasets. Matching seeds and
simulation arguments also pair separate GIC and BIC calls. When supplied,
the wrapper restores the caller's previous RNG state before returning.}

\item{store_studies}{Logical; if \code{TRUE}, retain the raw study objects for
every grid row and scenario. If \code{FALSE}, return only the summary table and
metadata.}
}
\value{
A list of class \code{bifrost_search_tuning_grid} with components:
\describe{
\item{\code{IC}}{The fixed IC family used across the grid.}
\item{\code{paired_settings}}{Always \code{TRUE}: datasets are paired across settings.}
\item{\code{study_seeds}}{Named integer vector of the shared null, proportional,
and correlation study seeds.}
\item{\code{grid}}{The evaluated combinations of thresholds and minimum clade
sizes.}
\item{\code{summary_table}}{A data frame with one row per setting, deterministic
per-study seed columns, and summary metrics from the null, proportional,
and correlation-scenario studies.}
\item{\code{simulation_generators}}{A named character vector recording the null,
proportional, and non-proportional simulation generators.}
\item{\code{studies}}{Either \code{NULL} or a list of raw study objects for each grid
row and scenario, depending on \code{store_studies}.}
\item{\code{base_search_options}}{The shared search options used before the
tuned grid parameters were injected.}
}
}
\description{
Run a small simulation-based tuning grid for one information-criterion
workflow at a time. This helper evaluates combinations of
\code{shift_acceptance_threshold} and \code{min_descendant_tips} under a fixed \code{IC}
value by combining:

\itemize{
\item a null false-positive study,
\item a proportional shift-recovery study, and
\item a non-proportional correlation robustness study.
}

The goal is to support practical tuning workflows such as "find the best
search settings under GIC" or "find the best search settings under BIC"
without treating \code{IC} as just another free parameter in one large
optimization.
}
\details{
This function is intentionally conservative in scope. It does not try to
optimize over \code{IC}; instead, it assumes that GIC and BIC should be tuned as
separate workflows. In typical use, you call it twice, once with
\code{IC = "GIC"} and once with \code{IC = "BIC"}, then select one recommended setting
from each grid with \code{\link[=selectTunedSearchParameters]{selectTunedSearchParameters()}}.

Within each scenario, every setting is evaluated on the same simulated
replicates: trees, traits, and planted shifts are paired across settings,
while different replicates remain independent simulation draws. The study
wrappers regenerate these datasets deterministically using shared seeds and
the L'Ecuyer-CMRG generator, so serial and parallel settings use the same
datasets. Simulation and search seeds are managed separately. Pairing also
applies when \code{seed = NULL}, but a new set of study seeds is drawn on each
call; supply an explicit seed to reproduce a grid or pair separate IC calls.
Changing grid order or adding thresholds does not change the simulated
datasets. Matching across calls requires the same template, simulation
options, replicate counts, and software/RNG configuration.

The template may come from a richer global calibration model, but each grid
row is still evaluated with an intercept-only shift search on the simulated
response block. This follows the residual-calibration workflow used by the
simulation-study wrappers. Covariates in the calibration model influence the
fitted means and residual covariance used for simulation, but they are not
re-fit inside each grid replicate.

The returned \code{summary_table} contains one row per grid combination. Null
summaries emphasize false-positive behavior, while proportional and
correlation-scenario summaries emphasize recovery, including fuzzy balanced
accuracy. The corresponding output columns retain their \code{correlation_}
prefixes for backward compatibility.
The \code{null_seed}, \code{proportional_seed}, and \code{correlation_seed} columns record
the shared study seeds, including when \code{seed = NULL}. These repeat across
grid rows rather than identifying independent datasets for each setting.
Candidate-set availability among completed searches is tracked via
evaluable fractions so that overly strict \code{min_descendant_tips} settings can
be screened out before choosing a final workflow. Completion and failure
rates use all attempted replicates and are reported separately. Scientific
means, proportions, and recovery summaries are computed among completed
searches, so failed searches are excluded from those summaries.

Parallelism is owned by the top-level function that the user calls. In this
helper, \code{num_cores} is interpreted at the setting level, so dependent study
wrappers are always called with \code{num_cores = 1} and their embedded search
calls are forced to \code{num_cores = 1} as well. This avoids nested parallelism
while keeping the user-facing API simple.
}
\examples{
\donttest{
set.seed(1)
tr <- ape::rtree(16)
X <- matrix(rnorm(16 * 2), ncol = 2)
rownames(X) <- tr$tip.label
tmpl <- createSimulationTemplate(tr, X, formula = "trait_data ~ 1", method = "LL")

gic_grid <- runSearchTuningGrid(
  template = tmpl,
  IC = "GIC",
  shift_acceptance_thresholds = 5,
  min_descendant_tips_values = 2,
  tree_tip_count = 14,
  null_replicates = 1,
  recovery_replicates = 1,
  null_simulation_options = list(simulation_generator = "empirical"),
  proportional_simulation_options = list(
    num_shifts = 1,
    min_shift_tips = 2,
    max_shift_tips = 5,
    scale_factor_range = c(5, 8),
    exclude_range = c(5.5, 6),
    buffer = 0,
    simulation_generator = "empirical"
  ),
  base_search_options = list(
    formula = "trait_data ~ 1",
    method = "LL",
    plot = FALSE,
    progress = FALSE,
    verbose = FALSE,
    store_model_fit_history = FALSE
  ),
  weighted = FALSE,
  num_cores = 1,
  seed = 2,
  store_studies = FALSE
)

gic_grid$summary_table[, c(
  "shift_acceptance_threshold",
  "min_descendant_tips",
  "null_mean_false_positive_rate",
  "proportional_evaluable_fraction",
  "proportional_strict_recall",
  "proportional_fuzzy_balanced_accuracy"
)]
}

}
\seealso{
\code{\link[=runFalsePositiveSimulationStudy]{runFalsePositiveSimulationStudy()}},
\code{\link[=runShiftRecoverySimulationStudy]{runShiftRecoverySimulationStudy()}}, \code{\link[=selectTunedSearchParameters]{selectTunedSearchParameters()}}
}
