% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rate-map-plot.R
\name{rateMapView}
\alias{rateMapView}
\title{Create a Display View of a \code{rateMap} Object}
\usage{
rateMapView(
  x,
  value = "value",
  palette = NULL,
  reverse_palette = NULL,
  color_mode = NULL,
  n_categories = NULL,
  category_bin_method = NULL,
  category_breaks = NULL,
  category_labels = NULL,
  ncolors = NULL,
  legend_title = NULL
)
}
\arguments{
\item{x}{An object of class \code{"rateMap"} returned by \code{\link[=rateMap]{rateMap()}}.}

\item{value}{Name of the numeric column in \code{x$intervals} to map to colors.
The default, \code{"value"}, uses the central summary computed by \code{\link[=rateMap]{rateMap()}}.}

\item{palette}{Optional palette override. This can be an \code{hcl.colors()}
palette name, a vector of colors, or a palette function.}

\item{reverse_palette}{Optional logical override for palette reversal.}

\item{color_mode}{Optional color-mode override. Use \code{"category"} for
discrete ordered bins or \code{"continuous"} for a continuous ramp.}

\item{n_categories}{Optional target category count for
\code{color_mode = "category"}.}

\item{category_bin_method}{Optional automatic category-binning method:
\code{"pretty"} or \code{"equal"}.}

\item{category_breaks}{Optional strictly increasing category boundaries.
Category legends draw valid numeric interval breaks with proportional
segment widths.}

\item{category_labels}{Optional labels for displayed categories.}

\item{ncolors}{Optional number of colors for continuous ramps. If omitted,
the stored \code{x$ncolors} value is used, falling back to \code{256} for older
objects.}

\item{legend_title}{Optional legend title stored on the returned object.}
}
\value{
A \code{"rateMap"} object with updated tree maps, color palette,
\code{intervals$value}, \code{rate_categories}, and legend title. For branch-summary
category views, \code{rate_categories} includes bin-level summaries of the
plotted branch values and is recomputed whenever the view changes. For
branch-summary maps with active rate diagnostics, diagnostic flags are
recomputed for rate-valued views (\code{"value"}, \code{"mean"}, or \code{"median"}) and
preserved as metadata for non-rate views such as \code{"sd"}. When preserved for
a non-rate view, \code{rate_flag_source} identifies the
rate-valued column that the flags classify; the flags do not classify the
displayed uncertainty value. The selected \code{value} column must contain
finite values for every plotted interval; uncertainty columns such as
\code{"sd"} may be all \code{NA} for single-fit objects and are rejected with a
clear error.
}
\description{
Recompute the display mapping for a computed \code{"rateMap"} object without
drawing it. This is optional in the usual \code{rateMap()} then \code{plot()} workflow;
use it when you want to save or inspect category tables, color bins, or an
uncertainty-valued map object and then reuse the same display mapping in one
or more plots. The numeric branch or interval summaries computed by
\code{\link[=rateMap]{rateMap()}} are not recomputed.
}
\examples{
\dontrun{
rm_obj <- rateMap(fits, uncertainty = TRUE)
display_obj <- rateMapView(
  rm_obj,
  palette = "Viridis",
  n_categories = 5,
  legend_title = "Mean log fitted rate"
)
plot(display_obj, type = "arc", show_tip_labels = FALSE)

sd_obj <- rateMapView(
  rm_obj,
  value = "sd",
  palette = "Inferno",
  color_mode = "continuous"
)
}

}
