% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/shift-distributions.R
\name{shift_node_marks}
\alias{shift_node_marks}
\alias{plot.shift_node_marks}
\alias{as.data.frame.shift_node_marks}
\title{Prepare And Plot Shift Node Marks On A Tree}
\usage{
shift_node_marks(
  x = NULL,
  tree = NULL,
  state_values = NULL,
  transitions = NULL,
  ic_weights = NULL,
  support_threshold = 0.9,
  letter_order = c("child_state", "node", "age"),
  marker_base_cex = 1.5,
  marker_scale_factor = 0.08,
  marker_transform = c("square_root", "log1p"),
  marker_max_cex = Inf
)

\method{plot}{shift_node_marks}(
  x,
  rate_changes = c("increase", "decrease"),
  show_letters = TRUE,
  show_low_support = TRUE,
  show_legend = TRUE,
  marker_alpha = 0.75,
  letter_cex = 0.75,
  increase_fill = "white",
  decrease_fill = "#2b6cb0",
  marker_col = "#111827",
  low_support_cex = 0.7,
  low_support_col = grDevices::adjustcolor("black", alpha.f = 0.58),
  legend_position = "topleft",
  legend_cex = 0.72,
  ...
)

\method{as.data.frame}{shift_node_marks}(
  x,
  row.names = NULL,
  optional = FALSE,
  component = c("marks", "increase_key", "summary"),
  ...
)
}
\arguments{
\item{x}{A \code{bifrost_search} object, a \code{shift_transitions()} table, a
SIMMAP-style \code{phylo} tree when \code{state_values} is supplied, or an object
previously returned by \code{shift_node_marks()}.}

\item{tree}{Optional SIMMAP-style \code{phylo} tree for generic input mode.}

\item{state_values}{Named numeric vector mapping SIMMAP state labels to
state-associated values for generic input mode.}

\item{transitions}{Optional precomputed transition table. When supplied,
\code{x} is used only as a possible source of \code{ic_weights}.}

\item{ic_weights}{Optional data frame with \code{node} and
\code{ic_weight_withshift} columns. When omitted for a compatible
\code{bifrost_search} object, \code{x$ic_weights} is used when available.}

\item{support_threshold}{Numeric threshold used to flag low-support shifts
from \code{ic_weight_withshift}.}

\item{letter_order}{Ordering used when assigning letters to increase nodes.
The default, \code{"child_state"}, matches the Berv et al. (2026) manuscript
convention by ordering increases by the destination regime state, with node
number as a stable tie-breaker. Use \code{"node"} for node-number order or
\code{"age"} for chronological order.}

\item{marker_base_cex, marker_scale_factor}{Controls for marker size. Size is
\code{marker_base_cex + transformed(abs(percentage_change)) * marker_scale_factor}.}

\item{marker_transform}{Transformation applied to absolute percent change
before scaling marker size.}

\item{marker_max_cex}{Maximum marker size.}

\item{rate_changes}{Which directional shifts to draw.}

\item{show_letters}{Logical; draw letter labels for increase nodes.}

\item{show_low_support}{Logical; draw an additional dot on shifts with
\code{ic_weight_withshift < support_threshold}.}

\item{show_legend}{Logical; draw a compact legend for the node marks.}

\item{marker_alpha}{Alpha used for filled node markers.}

\item{letter_cex}{Character expansion for letter labels.}

\item{increase_fill, decrease_fill}{Fill colors for increase and decrease
markers.}

\item{marker_col}{Outline color for filled node markers.}

\item{low_support_cex, low_support_col}{Size and color for low-support dots.}

\item{legend_position}{Position passed to \code{\link[graphics:legend]{graphics::legend()}}.}

\item{legend_cex}{Character expansion passed to \code{\link[graphics:legend]{graphics::legend()}}.}

\item{...}{Reserved for future extensions. Supplying unused arguments to
\code{plot()} or \code{as.data.frame()} methods is an error.}

\item{row.names, optional}{Included for compatibility with
\code{\link[base:as.data.frame]{base::as.data.frame()}}; ignored.}

\item{component}{Component to extract with \code{\link[base:as.data.frame]{base::as.data.frame()}}. Use
\code{"marks"}, \code{"increase_key"}, or \code{"summary"}.}
}
\value{
\code{shift_node_marks()} returns an object of class \code{"shift_node_marks"}
with \code{marks}, \code{increase_key}, \code{low_support_nodes}, \code{summary}, and
\code{settings}. Plotting returns the same object invisibly. The
\code{as.data.frame()} method returns the requested component as a plain
\code{data.frame}.
}
\description{
\code{shift_node_marks()} prepares node markers for inferred regime shifts on an
already plotted tree. For \code{bifrost_search} inputs, transitions are computed
from the final multi-regime BMM mapped tree. Generic workflows can supply a
\code{shift_transitions()} table, or a SIMMAP-style tree plus a named
\code{state_values} vector.
}
\details{
The workflow is intentionally plot-agnostic: first draw the tree with
\code{\link[=plot.rateMap]{plot.rateMap()}} or another tree plotting function, then call \code{plot()} on the
returned \code{shift_node_marks} object to add the node layer.
}
\examples{
toy_tree <- ape::read.tree(text = "(((a:1,b:1):1,c:2):1,d:3);")
toy_tree <- phytools::paintSubTree(
  tree = toy_tree,
  node = ape::Ntip(toy_tree) + 1L,
  state = "0"
)
toy_tree <- phytools::paintSubTree(
  tree = toy_tree,
  node = ape::Ntip(toy_tree) + 2L,
  state = "1"
)
toy_tree <- phytools::paintSubTree(
  tree = toy_tree,
  node = ape::Ntip(toy_tree) + 3L,
  state = "2"
)
marks <- shift_node_marks(
  tree = toy_tree,
  state_values = c("0" = 1, "1" = 4, "2" = 2)
)
marks$increase_key
}
