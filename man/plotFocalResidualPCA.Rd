% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/focal-disparity-test.R
\name{plotFocalResidualPCA}
\alias{plotFocalResidualPCA}
\title{Plot Residual Morphospace for a Focal Disparity Model}
\usage{
plotFocalResidualPCA(
  tree,
  trait_data,
  focal,
  formula = "trait_data ~ 1",
  model = "BM",
  method = "PL-LOOCV",
  pc_x = 1L,
  pc_y = 2L,
  center = TRUE,
  scale. = FALSE,
  focal_col = "#c44e52",
  background_col = "#4c72b0",
  focal_pch = 19,
  background_pch = 1,
  point_cex = 1,
  show_legend = TRUE,
  legend_position = "topright",
  focal_label = "Focal clade",
  background_label = "Background",
  use_shared_limits = TRUE,
  show_centroids = TRUE,
  centroid_cex = 1.6,
  centroid_pch = 4,
  centroid_lwd = 2,
  show_hulls = TRUE,
  hull_alpha = 0.12,
  hull_lwd = 1.2,
  layout = NULL,
  main = NULL,
  xlab = NULL,
  ylab = NULL,
  min_focal_tips = 3L,
  ...
)
}
\arguments{
\item{tree}{A rooted phylogeny of class \code{phylo} or coercible with
\code{\link[ape:as.phylo]{ape::as.phylo()}}.}

\item{trait_data}{A numeric matrix or data frame with row names matching tip
labels in \code{tree}.}

\item{focal}{Focal definition. Supply either a character vector of tip
labels, a logical selector of length \code{ape::Ntip(tree)}, or a single
internal node number.}

\item{formula}{Formula passed to \code{\link[mvMORPH:mvgls]{mvMORPH::mvgls()}}. Supports the same named,
response-only, and legacy indexed styles as \code{\link[=testFocalDisparity]{testFocalDisparity()}}.}

\item{model}{Character fit-model code or vector of fit-model codes. Supported
values are \code{"BM"}, \code{"OU"}, and \code{"EB"}. Supply multiple models, such as
\code{c("BM", "OU", "EB")}, to draw one residual morphospace panel per model.}

\item{method}{Character fitting method passed to \code{\link[mvMORPH:mvgls]{mvMORPH::mvgls()}}.}

\item{pc_x}{Integer principal-component index for the x-axis.}

\item{pc_y}{Integer principal-component index for the y-axis.}

\item{center}{Logical; passed to \code{\link[stats:prcomp]{stats::prcomp()}}.}

\item{scale.}{Logical; passed to \code{\link[stats:prcomp]{stats::prcomp()}}.}

\item{focal_col}{Color for focal taxa.}

\item{background_col}{Color for non-focal taxa.}

\item{focal_pch}{Plotting symbol for focal taxa.}

\item{background_pch}{Plotting symbol for non-focal taxa.}

\item{point_cex}{Point size passed to \code{\link[graphics:plot.default]{graphics::plot()}}.}

\item{show_legend}{Logical; if \code{TRUE}, draw a legend.}

\item{legend_position}{Legend position passed to \code{\link[graphics:legend]{graphics::legend()}}.}

\item{focal_label}{Legend label for focal taxa.}

\item{background_label}{Legend label for non-focal taxa.}

\item{use_shared_limits}{Logical; if \code{TRUE} and multiple models are drawn,
use shared x- and y-axis limits across all panels.}

\item{show_centroids}{Logical; if \code{TRUE}, draw group centroids.}

\item{centroid_cex}{Point size for centroid markers.}

\item{centroid_pch}{Plotting symbol used for centroid markers.}

\item{centroid_lwd}{Line width for centroid markers.}

\item{show_hulls}{Logical; if \code{TRUE}, draw convex hulls for focal and
background groups when enough points are available.}

\item{hull_alpha}{Alpha transparency used for hull fill colors.}

\item{hull_lwd}{Line width for hull borders.}

\item{layout}{Optional length-2 numeric vector giving the panel layout passed
to \code{\link[graphics:par]{graphics::par()}} as \code{mfrow} when multiple models are supplied. If
\code{NULL}, a sensible layout is chosen automatically.}

\item{main}{Plot title. Defaults to a model-aware residual morphospace title.
A single value is recycled across panels; a character vector of the same
length as \code{model} can supply panel-specific titles.}

\item{xlab}{X-axis label. Defaults to \code{PCk} plus explained variance. A single
value is recycled across panels; a character vector of the same length as
\code{model} can supply panel-specific labels.}

\item{ylab}{Y-axis label. Defaults to \code{PCk} plus explained variance. A single
value is recycled across panels; a character vector of the same length as
\code{model} can supply panel-specific labels.}

\item{min_focal_tips}{Minimum number of focal tips required after alignment.}

\item{...}{Additional arguments passed to \code{\link[mvMORPH:mvgls]{mvMORPH::mvgls()}}.}
}
\value{
For a single model, invisibly returns a list containing the fitted
\code{mvgls} object, the residual matrix, the \code{prcomp} result, the PC scores, a
logical focal-tip indicator, and the variance explained by each PC. For
multiple models, invisibly returns a named list of that same structure, one
entry per model.
}
\description{
Fit the same multivariate phylogenetic GLS model used by
\code{\link[=testFocalDisparity]{testFocalDisparity()}}, perform an ordinary PCA on the fitted residual
matrix with \code{\link[stats:prcomp]{stats::prcomp()}}, and plot focal versus background taxa in the
chosen residual morphospace dimensions.
}
\details{
This helper is intended as a descriptive follow-up to the focal disparity
test. It does not use \code{mvgls.pca}; instead it applies ordinary PCA to the
fitted residual matrix so the plotted point cloud corresponds directly to the
residual disparity space being tested. When multiple models are plotted, the
default behavior is to use shared axis limits so focal/background separation
can be compared directly across panels.
}
\examples{
\dontrun{
ex <- focalDisparityExampleData(seed = 2)

pca_plot <- plotFocalResidualPCA(
  tree = ex$tree,
  trait_data = ex$trait_data,
  focal = ex$focal_tips,
  formula = cbind(y1, y2, y3) ~ size,
  model = "OU",
  method = "LL"
)

head(pca_plot$scores)

pca_panels <- plotFocalResidualPCA(
  tree = ex$tree,
  trait_data = ex$trait_data,
  focal = ex$focal_tips,
  formula = cbind(y1, y2, y3) ~ size,
  model = c("BM", "OU", "EB"),
  method = "LL"
)
}

}
