% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/focal-disparity-test.R
\name{testFocalDisparity}
\alias{testFocalDisparity}
\title{Test Focal Disparity by Parametric Bootstrap}
\usage{
testFocalDisparity(
  tree,
  trait_data,
  focal,
  formula = "trait_data ~ 1",
  metric = c("centroid_msd", "trace_cov", "mean_pairwise_sqdist"),
  nsim = 999,
  model = "BM",
  method = "PL-LOOCV",
  workers = future::availableCores(),
  seed = 1,
  future_plan = NULL,
  show_progress = TRUE,
  min_focal_tips = 3L,
  ...
)
}
\arguments{
\item{tree}{A rooted phylogeny of class \code{phylo} or coercible with
\code{\link[ape:as.phylo]{ape::as.phylo()}}.}

\item{trait_data}{A numeric matrix or data frame with row names matching tip
labels in \code{tree}. Extra rows or tree tips are dropped by intersection.}

\item{focal}{Focal definition. Supply either a character vector of tip labels,
a logical selector of length \code{ape::Ntip(tree)}, or a single internal node
number whose descendant tips define the focal clade.}

\item{formula}{Formula passed to \code{\link[mvMORPH:mvgls]{mvMORPH::mvgls()}}. Named-column formulas
such as \code{cbind(y1, y2) ~ size + group} are supported, as are response-only
matrix workflows such as \code{"trait_data ~ 1"}.}

\item{metric}{Either one of \code{"centroid_msd"}, \code{"trace_cov"}, or
\code{"mean_pairwise_sqdist"}, or a function that accepts the focal residual
matrix and returns a single numeric value. The built-in options are all
disparity/dispersion summaries.}

\item{nsim}{Integer number of bootstrap replicates.}

\item{model}{Character \code{mvgls} fit-model code used for the observed fit,
bootstrap simulations, and each bootstrap refit. Supported values are
\code{"BM"}, \code{"OU"}, and \code{"EB"}.}

\item{method}{Character fitting method passed to \code{\link[mvMORPH:mvgls]{mvMORPH::mvgls()}}.}

\item{workers}{Integer number of workers used when \code{future_plan} is
\code{"multisession"} or \code{"multicore"}.}

\item{seed}{Integer seed passed to \code{future.apply} for reproducible bootstrap
streams.}

\item{future_plan}{Future backend used for bootstrap refits. One of
\code{"multisession"}, \code{"multicore"}, or \code{"sequential"}. If \code{NULL}, the
function uses \code{"sequential"} when \code{workers = 1} and \code{"multisession"}
otherwise.}

\item{show_progress}{Logical; if \code{TRUE}, wrap bootstrap evaluation in
\code{\link[progressr:with_progress]{progressr::with_progress()}}. Users can set their preferred progress
handler with \code{progressr::handlers()}.}

\item{min_focal_tips}{Minimum number of focal tips required after alignment.}

\item{...}{Additional arguments passed to \code{\link[mvMORPH:mvgls]{mvMORPH::mvgls()}} for both the
observed fit and bootstrap refits.}
}
\value{
A list of class \code{bifrost_focal_disparity_test} containing the aligned
data, the fitted \code{mvgls} object, the focal tip set, the observed statistic,
the bootstrap null distribution, and standardized effect-size and p-value
summaries.
}
\description{
Fit a multivariate phylogenetic GLS model with \code{\link[mvMORPH:mvgls]{mvMORPH::mvgls()}}, compute a
focal disparity statistic on the residuals of a focal set of tips, and
compare that observed value to a parametric bootstrap null distribution.
}
\details{
The bootstrap null simulates a residual process under the same single-regime
model used for fitting: \code{"BM"} uses \code{mvSIM(model = "BM1")}, \code{"OU"} uses
\code{mvSIM(model = "OU1")}, and \code{"EB"} uses \code{mvSIM(model = "EB")}. The fitted
residual covariance matrix and fitted mean structure from the observed
\code{mvgls} model are reused in each bootstrap replicate.

If \code{focal} is supplied as an internal node number, descendant tips are
resolved on the original tree before overlap pruning, so the focal definition
remains stable when non-overlapping taxa are dropped during alignment.
}
\examples{
\dontrun{
ex <- focalDisparityExampleData(seed = 2)

res <- testFocalDisparity(
  tree = ex$tree,
  trait_data = ex$trait_data,
  focal = ex$focal_tips,
  formula = cbind(y1, y2, y3) ~ size,
  metric = "centroid_msd",
  nsim = 100,
  model = "BM",
  method = "LL",
  workers = 1,
  future_plan = "sequential",
  show_progress = FALSE,
  seed = 2
)

ex$focal_node
length(ex$focal_tips)
print(res)
plot(res)
}

}
\seealso{
\code{\link[=createSimulationTemplate]{createSimulationTemplate()}}
}
