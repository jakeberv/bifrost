% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rate-map.R
\name{rateMapRateFlags}
\alias{rateMapRateFlags}
\title{Control Near-Zero and Tail Rate Diagnostics for \code{rateMap()}}
\usage{
rateMapRateFlags(
  near_zero = TRUE,
  high_outlier = FALSE,
  method = NULL,
  zero_floor = NULL,
  cluster_min_log_gap = log(10),
  cluster_min_fold_reduction = 10,
  cluster_max_tail_fraction = 0.4,
  cluster_min_flagged = 3,
  cluster_min_regular = 10,
  zero_label = "near-zero",
  high_label = "high-outlier",
  zero_color = "grey70",
  high_color = "black"
)
}
\arguments{
\item{near_zero}{Logical; if \code{TRUE}, flag lower-tail or floor-level rates.}

\item{high_outlier}{Logical; if \code{TRUE}, flag isolated upper-tail rates.}

\item{method}{Optional detection method. \code{NULL} chooses \code{"floor"} when
\code{zero_floor} is supplied and \code{"none"} otherwise. \code{"floor"} uses
\code{zero_floor} for near-zero rates. \code{"tail_cluster"} uses an Otsu-style
guarded two-class split of log rates to identify a separated lower-tail
cluster, and, when \code{high_outlier = TRUE}, a separated upper-tail cluster.
\code{"none"} computes diagnostics without adding special rate flags.}

\item{zero_floor}{Optional non-negative rate floor. When supplied with
\code{method = NULL}, the method resolves to \code{"floor"}. Finite rates less than
or equal to this value are flagged as near-zero.}

\item{cluster_min_log_gap}{Minimum log-rate gap required between a
cluster-defined tail and the remaining regular rates.}

\item{cluster_min_fold_reduction}{Minimum fold-range reduction required after
separating a cluster-defined tail from the regular rates.}

\item{cluster_max_tail_fraction}{Maximum fraction of positive finite rates
that can be assigned to a cluster-defined tail.}

\item{cluster_min_flagged}{Minimum number of branches required for a
cluster-defined tail.}

\item{cluster_min_regular}{Minimum number of branches that must remain in the
regular rate set after separating a cluster-defined tail.}

\item{zero_label, high_label}{Labels used for flagged display categories.}

\item{zero_color, high_color}{Colors used for flagged display categories in
category mode.}
}
\value{
A \code{"rateMap_rate_flags"} object for \code{rateMapControl(rate_flags = )}.
}
\description{
Build a rate-flagging control object for \code{\link[=rateMapControl]{rateMapControl()}}. These options
identify branch-rate summaries that are effectively zero, or optionally in a
separated high-rate tail, without deleting or changing the fitted rates. Flags
are reported in the returned \code{intervals} table and summarized in
\code{rate_diagnostics}.
}
\examples{
\dontrun{
ctrl <- rateMapControl(rate_flags = rateMapRateFlags(zero_floor = 1e-8))
rm_obj <- rateMap(fits, control = ctrl)
rm_obj$rate_diagnostics

cluster_ctrl <- rateMapControl(
  rate_flags = rateMapRateFlags(method = "tail_cluster")
)
rm_obj <- rateMap(fits, control = cluster_ctrl)
rm_obj$rate_diagnostics
}

}
\references{
Otsu, N. (1979). A threshold selection method from gray-level histograms.
\emph{IEEE Transactions on Systems, Man, and Cybernetics}, 9(1), 62-66.
\doi{10.1109/TSMC.1979.4310076}
}
