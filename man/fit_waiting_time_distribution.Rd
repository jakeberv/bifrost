% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/shift-distributions.R
\name{fit_waiting_time_distribution}
\alias{fit_waiting_time_distribution}
\title{Fit Candidate Distributions To Shift Waiting Times}
\usage{
fit_waiting_time_distribution(
  waiting_times,
  by_lineage = FALSE,
  global_include_root_wait = TRUE,
  models = c("exp", "gamma", "weibull", "lnorm", "invgauss"),
  select_by = c("AIC", "BIC")
)
}
\arguments{
\item{waiting_times}{Numeric vector, waiting-time data frame,
\code{shift_waiting_times} object, or BMM \code{bifrost_search} output.}

\item{by_lineage}{Logical; when \code{TRUE}, use pooled between-shift lineage
intervals for the top-level fit instead of chronological whole-tree
intervals, and also fit candidate distributions separately for each
lineage with at least two waiting times.}

\item{global_include_root_wait}{Logical; when fitting whole-tree global waits
from a \code{shift_waiting_times} object, \code{bifrost_search} object, or global
waiting-time data frame, include the initial root-to-first-shift interval.
The default \code{TRUE} matches the Berv et al. (2026) reported whole-tree
Weibull summary. Set to \code{FALSE} to exclude the row labeled
\code{PreviousShift == "root"}. Multiple such rows are ambiguous and produce an
error; data without that label are unchanged.}

\item{models}{Character vector of positive-support \code{univariateML} model
identifiers.}

\item{select_by}{Criterion used to select best models.}
}
\value{
A list of class \code{waiting_time_distribution_fit} with pooled model
rankings and a long-form \code{parameters} table of fitted parameter estimates.
With \code{by_lineage = FALSE}, the top-level pool contains chronological
whole-tree intervals; with \code{by_lineage = TRUE}, it contains pooled
between-shift lineage intervals and the result additionally includes
per-lineage best-model, parameter, and exponential-rate summaries. The raw
\code{univariateML} objects remain available in \code{fits} and \code{lineage_fits}.
}
\description{
Fit and rank candidate positive continuous distributions for shift waiting
times. Inputs can be numeric waiting times, data frames with a \code{TimeToNext}
column, BMM \code{bifrost_search} output, or the output of
\code{shift_waiting_times()}. Fitted \code{bifrost_search} inputs are accepted only
for multi-regime BMM fits; numeric vectors and data frames remain generic.
Because the candidate distributions have strictly positive support, inputs
containing zero waits from tied or simultaneous events are rejected with an
explanatory error; the zero waits remain available from
\code{\link[=shift_waiting_times]{shift_waiting_times()}} for descriptive analysis.
}
\examples{
if (requireNamespace("univariateML", quietly = TRUE)) {
  waits <- c(0.5, 0.8, 1.2, 1.9, 2.4, 3.0)
  fit_waiting_time_distribution(waits, models = c("exp", "gamma", "weibull"))
}
}
