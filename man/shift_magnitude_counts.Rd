% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/shift-distributions.R
\name{shift_magnitude_counts}
\alias{shift_magnitude_counts}
\title{Count Inferred Rate Increases And Decreases}
\usage{
shift_magnitude_counts(x = NULL, tree = NULL, state_values = NULL)
}
\arguments{
\item{x}{A transition table, \code{bifrost_search} object, list of
transition/search objects, \code{shift_magnitude_comparison} object,
\code{shift_magnitude_groups} object, or \code{NULL} when using the \code{tree} argument.}

\item{tree}{Optional SIMMAP-style \code{phylo} tree for generic input mode.}

\item{state_values}{Named numeric state-value vector for generic input mode.}
}
\value{
A \code{shift_magnitude_counts} data frame with one row per input run.
Columns include \code{source}, increase/decrease counts, total directional
shifts, and increase/decrease frequencies. When \code{x} is a
\code{shift_magnitude_groups} object, a named \code{shift_magnitude_count_set} is
returned instead.
}
\description{
Count inferred rate increases and decreases for a transition table,
\code{bifrost_search} object, \code{shift_magnitude_comparison} object, or list of
runs. This is the tabular input to \code{plot()} for shift-magnitude counts and
reproduces the count/frequency summaries used by Berv et al. (2026) before
plotting. Fitted \code{bifrost_search} inputs are accepted only for multi-regime
BMM fits; generic mapped-tree inputs should use \code{tree} plus \code{state_values}.
}
\examples{
transitions <- data.frame(
  rate_change = c("root", "increase", "increase", "decrease", "decrease"),
  rate_delta = c(NA, 3, 4, -1, -2)
)
shift_magnitude_counts(list(run1 = transitions, run2 = transitions))
shift_magnitude_counts(shift_magnitude_groups(A = transitions, B = transitions))
}
