% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/shift-distributions.R
\name{plot.shift_magnitude_counts}
\alias{plot.shift_magnitude_counts}
\title{Plot One Shift Count Or Frequency Comparison}
\usage{
\method{plot}{shift_magnitude_counts}(
  x,
  statistic = c("frequency", "count"),
  colors = c(increase = "#377eb8", decrease = "#e41a1c"),
  point_cex = 1.15,
  line_col = grDevices::adjustcolor("grey35", alpha.f = 0.35),
  boxplot = TRUE,
  paired_lines = TRUE,
  paired_test = TRUE,
  reference_line = NULL,
  ylim = NULL,
  ylab = NULL,
  main = NULL,
  mar = c(4.5, 4.2, 2.4, 0.8),
  oma = c(0, 0, 0, 0),
  ...
)
}
\arguments{
\item{x}{A \code{shift_magnitude_counts} object.}

\item{statistic}{Plot \code{"frequency"} or raw \code{"count"} values.}

\item{colors}{Named colors for \code{"increase"} and \code{"decrease"}.}

\item{point_cex}{Point size for run-level observations.}

\item{line_col}{Color for paired run-level segments.}

\item{boxplot}{Logical; overlay compact boxplots.}

\item{paired_lines}{Logical; draw paired decrease-to-increase segments for
each run.}

\item{paired_test}{Logical; annotate the plot with a paired Wilcoxon test
when at least two runs are available.}

\item{reference_line}{Optional horizontal reference line. Defaults to \code{0.5}
for frequencies and no line for counts.}

\item{ylim}{Optional common y-axis limits.}

\item{ylab, main}{Plot labels.}

\item{mar, oma}{Graphical margins passed to \code{\link[graphics:par]{graphics::par()}}.}

\item{...}{Additional graphical parameters passed to \code{\link[graphics:plot]{graphics::plot()}}.}
}
\value{
Invisibly returns a list with the count data, paired tests, and plot
settings.
}
\description{
Summarize inferred rate increases and decreases for one set of search
outputs. The default paired frequency plot mirrors the count/frequency
comparison used for Berv et al. (2026) Supplementary Figure 5.
}
\examples{
transitions <- data.frame(
  rate_change = c("root", "increase", "increase", "decrease", "decrease"),
  rate_delta = c(NA, 3, 4, -1, -2)
)
counts <- shift_magnitude_counts(list(run1 = transitions, run2 = transitions))
plot(counts)
}
