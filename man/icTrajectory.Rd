% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/icTrajectory.R
\name{icTrajectory}
\alias{icTrajectory}
\title{Create an Information-Criterion Trajectory}
\usage{
icTrajectory(x, baseline_ic = NULL, ...)
}
\arguments{
\item{x}{A \code{bifrost_search} object returned by \code{\link[=searchOptimalConfiguration]{searchOptimalConfiguration()}}
with \code{store_model_fit_history = TRUE}, or a compatible list with
\code{baseline_ic} and \code{model_fit_history}.}

\item{baseline_ic}{Optional finite numeric baseline IC. When supplied, this
value is used for \code{step = 0} instead of \code{x$baseline_ic}. This is useful for
legacy search-like lists that did not store \code{baseline_ic}.}

\item{...}{Reserved for future extensions.}
}
\value{
A data frame with class \code{c("icTrajectory", "data.frame")}.
}
\description{
Extract a standardized information-criterion (IC) trajectory from a
completed \code{bifrost_search} object or a compatible search-result list.
}
\details{
\code{icTrajectory()} is a lightweight extractor. It does not refit models or
recompute the search. The baseline IC is taken from \code{x$baseline_ic} unless
\code{baseline_ic} is supplied, and is always included as \code{step = 0}; later rows
summarize the stored proposal history in \code{x$model_fit_history$fits}. Legacy
objects that only contain \code{x$model_fit_history$ic_acceptance_matrix}, or
legacy \code{fits} entries without explicit proposal metadata, are also
supported. In those legacy cases, \code{candidate_node} cannot be recovered, and
\code{regime_id} is inferred from proposal order because Bifrost assigns
candidate shift regimes sequentially during the search.

The returned object is a data frame with class
\code{c("icTrajectory", "data.frame")} and the following columns:
\itemize{
\item \code{step}: search step, with \code{0} reserved for the baseline.
\item \code{ic}: IC for the baseline or evaluated proposal.
\item \code{accepted}: proposal decision; \code{NA} for the baseline.
\item \code{best_ic}: running best IC after each step.
\item \code{delta_ic}: proposal improvement over the current best before the
proposal; positive values indicate improvement.
\item \code{status}: \code{"baseline"}, \code{"accepted"}, \code{"rejected"}, or \code{"error"}.
\item \code{candidate_node}: phylogenetic node where the shift was evaluated.
\item \code{regime_id}: mapped-state/regime label assigned to the evaluated
candidate shift. The baseline row uses \code{"0"}.
\item \code{error}: optional error message.
}
}
\examples{
search <- list(
  baseline_ic = -1000,
  IC_used = "GIC",
  model_fit_history = list(
    fits = list(
      list(step = 1, candidate_node = 42, regime_id = "1", ic = -1010, accepted = TRUE, delta_ic = 10),
      list(step = 2, candidate_node = 57, regime_id = "2", ic = -1008, accepted = FALSE, delta_ic = -2)
    )
  )
)
class(search) <- c("bifrost_search", "list")
traj <- icTrajectory(search)
legacy_traj <- icTrajectory(search, baseline_ic = -995)
plot(traj)

}
