% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/shift-distributions.R
\name{bootstrap_rate_distribution}
\alias{bootstrap_rate_distribution}
\title{Bootstrap A Fitted Rate Distribution}
\usage{
bootstrap_rate_distribution(
  x,
  model = NULL,
  reps = 1000L,
  seed = NULL,
  kl = NULL
)
}
\arguments{
\item{x}{A \code{rate_distribution_fit} object from \code{\link[=fit_rate_distribution]{fit_rate_distribution()}}.}

\item{model}{Distribution model to bootstrap. If \code{NULL}, the selected model
stored in \code{x$selected_model} is used.}

\item{reps}{Number of bootstrap parameter draws.}

\item{seed}{Optional random seed for the bootstrap draw. The previous global
RNG state is restored after the bootstrap when a seed is supplied.}

\item{kl}{\code{NULL} or logical. When \code{NULL}, compute the bootstrap
Kullback-Leibler divergence summary for Gumbel fits and skip it for other
models. Set to \code{FALSE} to store only the bootstrap parameter draws.}
}
\value{
A \code{rate_distribution_fit} object with a
\code{rate_distribution_bootstrap} entry stored in \code{x$bootstrap[[model]]}.
The entry contains the model name, number of draws, seed, and parameter
matrix returned by \code{\link[univariateML:bootstrapml]{univariateML::bootstrapml()}}. For Gumbel fits, the
entry also stores a bootstrapped Kullback-Leibler divergence summary unless
\code{kl = FALSE}.
}
\description{
Precompute parametric bootstrap parameter draws for a fitted rate
distribution. This keeps stochastic uncertainty estimation out of the plotting
method while preserving the bootstrap draws on the returned
\code{rate_distribution_fit} object.
}
\details{
For Gumbel fits, the default \code{kl = NULL} also computes the
Kullback-Leibler divergence between the empirical kernel density of the
fitted data and the fitted Gumbel density, then repeats that calculation for
each bootstrap parameter draw. Extract the compact diagnostic table with
\code{as.data.frame(x, component = "goodness")}.
}
\examples{
if (requireNamespace("univariateML", quietly = TRUE) &&
    requireNamespace("evd", quietly = TRUE)) {
  fit <- fit_rate_distribution(c(1, 1.4, 1.9, 2.8, 4.1, 6.5), models = "gumbel")
  fit <- bootstrap_rate_distribution(fit, model = "gumbel", reps = 25, seed = 1)
}
}
