% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/shift-distributions.R
\name{compare_shift_magnitudes}
\alias{compare_shift_magnitudes}
\title{Compare Rate-Shift Magnitudes}
\usage{
compare_shift_magnitudes(
  x = NULL,
  tree = NULL,
  state_values = NULL,
  measure = c("rate_delta", "percentage_change", "log_ratio"),
  transform = c("log_absolute", "absolute", "signed"),
  tests = "ks",
  alternative = c("two.sided", "less", "greater"),
  ks_simulate_p_value = TRUE,
  ks_B = 10000L,
  bootstrap_p_value = FALSE,
  bootstrap_R = 100L,
  ks_reps = NULL,
  bootstrap_reps = NULL,
  seed = NULL
)
}
\arguments{
\item{x}{A \code{bifrost_search} object, compatible list, SIMMAP tree, data
frame with \code{rate_change} and the selected \code{measure}, a list of transition
tables or BMM \code{bifrost_search} objects to pool before testing, a
\code{shift_magnitude_groups} object to compare each named group separately,
or \code{NULL} when using the \code{tree} argument. Fitted \code{bifrost_search} inputs
are accepted only for multi-regime BMM fits.}

\item{tree}{Optional SIMMAP-style \code{phylo} tree for generic input mode.}

\item{state_values}{Named numeric state-value vector for generic input mode.}

\item{measure}{Which transition column to compare. \code{rate_delta} compares
fitted child-minus-parent rate differences, \code{percentage_change} compares
percent changes relative to the parent rate, and \code{log_ratio} compares log
child/parent rate ratios.}

\item{transform}{How to transform the selected measure before testing.
\code{"absolute"} compares magnitudes, \code{"log_absolute"} compares log
magnitudes, and \code{"signed"} compares signed values.}

\item{tests}{Character vector of tests to run. Supported values are \code{"t"}
for a Welch t-test, \code{"wilcox"} for an unpaired Wilcoxon rank-sum test, and
\code{"ks"} for a two-sample Kolmogorov-Smirnov test. Use \code{"all"} for all
three. The default reproduces the Berv et al. (2026) rate-delta magnitude
comparison: a KS test on \code{log(abs(rate_delta))}.}

\item{alternative}{Alternative hypothesis passed to the tests.}

\item{ks_simulate_p_value}{Logical; passed to \code{\link[stats:ks.test]{stats::ks.test()}} when
\code{"ks"} is requested. The default matches the Berv et al. (2026) utility.}

\item{ks_B}{Integer number of replicates for the simulated KS p-value.}

\item{bootstrap_p_value}{Logical; estimate the mean p-value from stratified
bootstrap resampling within the increase and decrease groups. This
reproduces the bootstrap p-value annotation used in the Berv et al. (2026)
magnitude-density panels when \code{"ks"} is requested.}

\item{bootstrap_R}{Integer number of bootstrap replicates when
\code{bootstrap_p_value = TRUE}.}

\item{ks_reps}{Optional clearer alias for \code{ks_B}.}

\item{bootstrap_reps}{Optional clearer alias for \code{bootstrap_R}.}

\item{seed}{Optional random seed for stochastic KS simulation and
bootstrap p-value resampling. The Berv et al. (2026) defaults are unchanged;
when a seed is supplied, the previous global RNG state is restored before
returning.}
}
\value{
A list of class \code{shift_magnitude_comparison} with the transformed
values used for testing, group summaries, modal summaries, test results,
and settings. When \code{x} is a \code{shift_magnitude_groups} object, a named
\code{shift_magnitude_comparison_set} is returned instead.
}
\description{
Compare the magnitude of fitted rate increases and decreases from a
\code{bifrost_search} BMM result or a transition table returned by
\code{shift_transitions()}. This is the package-level analogue of the Berv et al.
(2026) workflow that summarized rate changes and tested whether increase
and decrease magnitudes differed. When \code{x} is a \code{bifrost_search} object, it
must be a multi-regime BMM fit; use a transition table or \code{tree} plus
\code{state_values} for generic mapped-tree inputs.
}
\details{
Plain lists of transition tables or BMM search objects are pooled into one
comparison before testing. Use \code{\link[=shift_magnitude_groups]{shift_magnitude_groups()}} when list elements
are analysis groups that should be compared separately. If a group is already
a \code{shift_magnitude_comparison}, its \code{measure}, \code{transform}, \code{tests},
\code{alternative}, KS settings, and bootstrap settings must exactly match the
grouped call; otherwise, recompute that group from raw transitions or call
\code{compare_shift_magnitudes()} with matching settings.
}
\examples{
transitions <- data.frame(
  rate_change = c("increase", "increase", "decrease", "decrease"),
  rate_delta = c(2, 3, -1, -1.5)
)
compare_shift_magnitudes(transitions, ks_reps = 99)
groups <- shift_magnitude_groups(A = transitions, B = transitions)
compare_shift_magnitudes(groups, ks_reps = 99)
}
