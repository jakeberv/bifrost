% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/regime-integration.R
\name{regime_integration_relationships}
\alias{regime_integration_relationships}
\title{Analyze Rate-Integration Relationships Across Regimes}
\usage{
regime_integration_relationships(
  summaries,
  resid_sd_threshold_vars = 2,
  resid_sd_threshold_corrs = 2,
  n_boot = 1000,
  ci_level = 0.99,
  seed = NULL,
  min_tips = NULL
)
}
\arguments{
\item{summaries}{A list of manuscript-compatible \code{vars_cors} tables or a data
frame from \code{\link[=summarize_regime_covariances]{summarize_regime_covariances()}}. Regime IDs within each input
table must be unique and non-empty. List inputs are pooled with a \code{run}
column; missing or blank list names become \code{run1}, \code{run2}, and so on based
on their positions, and the normalized names must be unique.}

\item{resid_sd_threshold_vars, resid_sd_threshold_corrs}{Non-negative finite
studentized-residual thresholds used to filter the variance and
correlation panels.}

\item{n_boot}{Number of bootstrap replicates for confidence curves.}

\item{ci_level}{Confidence level for bootstrap ribbons.}

\item{seed}{Optional random seed for reproducible bootstrap curves.}

\item{min_tips}{Optional minimum tip count for inclusion when summaries
contain a \code{tip_count} column. Relationship summaries are retained when
\code{tip_count >= min_tips}; this differs from the strict PCA manuscript filter
used by \code{\link[=regime_correlation_pca]{regime_correlation_pca()}}.}
}
\value{
An object of class \code{regime_integration_relationships}, containing
plot-ready data frames, fitted \code{lm} objects, bootstrap curves, and settings.
}
\description{
Prepare the point sets and bootstrap confidence curves used for the
Supplementary Figure 4A-style rate-vs-variance and rate-vs-integration
panels. Use \code{plot()} on the returned object to draw the panels.
}
\details{
High-correlation filtering is applied before this step with
\code{\link[=summarize_regime_covariances]{summarize_regime_covariances()}} via \code{remove_high_corr} and
\code{corr_threshold}. The relationship object records residual filters and
bootstrap settings, but it does not reapply matrix-level correlation filters.
Rows with incomplete data for a panel remain in \code{combined} with an \code{NA}
studentized residual and are omitted from that panel's point and removed-row
data frames.
}
