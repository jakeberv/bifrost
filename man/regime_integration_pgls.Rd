% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/regime-integration.R
\name{regime_integration_pgls}
\alias{regime_integration_pgls}
\title{Fit the Manuscript-style Regime Integration pGLS}
\usage{
regime_integration_pgls(
  summary_data,
  search = NULL,
  tree = NULL,
  model = "BM",
  min_tips = NULL,
  ...
)
}
\arguments{
\item{summary_data}{A data frame from \code{\link[=summarize_regime_covariances]{summarize_regime_covariances()}} or a
manuscript-compatible \code{vars_cors} table with columns \code{rate}, \code{vars},
\code{corrs}, and \code{State}. Regime IDs must be unique and non-empty.}

\item{search}{A \code{bifrost_search} object containing the mapped regime tree.}

\item{tree}{Optional SIMMAP-style mapped tree. Ignored when \code{search} is
supplied.}

\item{model}{Evolutionary model passed to \code{\link[phylolm:phylolm]{phylolm::phylolm()}}. Defaults to
\code{"BM"}, matching the manuscript.}

\item{min_tips}{Optional minimum tip count for downstream inclusion when
\code{summary_data} contains a \code{tip_count} column. Downstream summaries are
retained when \code{tip_count >= min_tips}; this differs from the strict PCA
manuscript filter used by \code{\link[=regime_correlation_pca]{regime_correlation_pca()}}.}

\item{...}{Additional arguments passed to \code{\link[phylolm:phylolm]{phylolm::phylolm()}}.}
}
\value{
A \code{phylolm} fit.
}
\description{
Reproduce the representative pGLS test used in the post-hoc integration
analysis: log regime rate is modeled as a function of log post-hoc mean
variance and Fisher-Z transformed mean absolute trait correlation on a
collapsed regime phylogeny.
}
\details{
The collapse step follows the manuscript implementation: a
monophyletic regime is represented by one collapsed tip, whereas all tips
assigned to a nonmonophyletic regime are removed. When removals occur, the
function emits one warning listing every dropped regime ID. The collapse
stops if regime relabeling would create duplicated output tip labels.
}
