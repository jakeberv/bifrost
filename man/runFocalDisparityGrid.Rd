% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/focal-disparity-test.R
\name{runFocalDisparityGrid}
\alias{runFocalDisparityGrid}
\title{Run a Full Focal Disparity Grid Across Models and Metrics}
\usage{
runFocalDisparityGrid(
  data_bundle,
  predictor_col = 1L,
  response_cols = 2:12,
  metrics = c("centroid_msd", "trace_cov", "mean_pairwise_sqdist", "log_det_cov",
    "max_eigenvalue", "eigenvalue_evenness"),
  models = c("BM", "OU", "EB"),
  nsim = 1000L,
  method = "LL",
  workers = 1L,
  seed = 2L,
  future_plan = NULL,
  show_progress = TRUE,
  min_focal_tips = 3L,
  output_dir = NULL,
  plot_width = 7,
  plot_height = 5,
  plot_dpi = 150,
  ...
)
}
\arguments{
\item{data_bundle}{A list with components \code{tree}, \code{data}, and \code{focal_tips}.
\code{data} should be a matrix or data frame with row names matching tree tip
labels.}

\item{predictor_col}{Integer index of the predictor column used on the right
side of the generated \code{mvgls} formula.}

\item{response_cols}{Integer vector of response columns used on the left side
of the generated \code{cbind(...)} formula.}

\item{metrics}{Character vector of metric names passed to
\code{\link[=testFocalDisparity]{testFocalDisparity()}}.}

\item{models}{Character vector of supported fit/simulation models passed to
\code{\link[=testFocalDisparity]{testFocalDisparity()}}.}

\item{nsim}{Integer number of bootstrap replicates per model-metric
combination.}

\item{method}{Character fitting method passed to \code{\link[mvMORPH:mvgls]{mvMORPH::mvgls()}}.}

\item{workers}{Integer number of bootstrap workers.}

\item{seed}{Integer seed passed through to each grid cell run.}

\item{future_plan}{Future backend used for each grid cell. If \code{NULL}, the
same defaulting logic as \code{\link[=testFocalDisparity]{testFocalDisparity()}} is used.}

\item{show_progress}{Logical; if \code{TRUE}, show per-run bootstrap progress.}

\item{min_focal_tips}{Minimum number of focal tips required after alignment.}

\item{output_dir}{Optional directory for saving a CSV summary, an \code{.rds}
object containing the individual results, and one PNG plot per
model-metric combination. If \code{NULL}, no files are written.}

\item{plot_width}{Plot width in inches for saved PNG outputs.}

\item{plot_height}{Plot height in inches for saved PNG outputs.}

\item{plot_dpi}{Plot resolution for saved PNG outputs.}

\item{...}{Additional arguments passed to \code{\link[=testFocalDisparity]{testFocalDisparity()}} and onward
to \code{\link[mvMORPH:mvgls]{mvMORPH::mvgls()}}.}
}
\value{
An object of class \code{bifrost_focal_disparity_grid} containing the
constructed formula, the chosen predictor and responses, a summary data
frame ordered by upper-tail p-value, the raw list of
\code{\link[=testFocalDisparity]{testFocalDisparity()}} result objects, and the normalized output directory
path when files are written.
}
\description{
Evaluate \code{\link[=testFocalDisparity]{testFocalDisparity()}} across all requested combinations of
supported single-regime models and focal disparity metrics for a shared data
bundle.
}
\details{
The generated formula has the form
\code{cbind(response_1, response_2, ...) ~ predictor}, where the predictor and
responses are selected from \code{data_bundle$data} using \code{predictor_col} and
\code{response_cols}.
}
\examples{
\dontrun{
ex <- focalDisparityExampleData(seed = 2)

grid <- runFocalDisparityGrid(
  data_bundle = list(
    tree = ex$tree,
    data = ex$trait_data,
    focal_tips = ex$focal_tips
  ),
  predictor_col = 4,
  response_cols = 1:3,
  nsim = 25,
  models = c("BM", "OU"),
  metrics = c("centroid_msd", "mean_pairwise_sqdist"),
  workers = 1,
  future_plan = "sequential",
  show_progress = FALSE
)

grid$summary
}

}
