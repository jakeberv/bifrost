% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulation-framework.R
\name{simulateNullDataset}
\alias{simulateNullDataset}
\title{Simulate a Dataset-Matched Null Replicate}
\usage{
simulateNullDataset(
  template,
  tree_tip_count = NULL,
  seed = NULL,
  preserve_predictors = TRUE,
  ...
)
}
\arguments{
\item{template}{A \code{bifrost_simulation_template} returned by
\code{\link[=createSimulationTemplate]{createSimulationTemplate()}}.}

\item{tree_tip_count}{Optional integer tip count for random subtree sampling.
If \code{NULL}, the full empirical tree from \code{template} is used.}

\item{seed}{Optional integer random seed.}

\item{preserve_predictors}{Logical; if \code{TRUE}, predictor columns stored in the
template are carried through unchanged for the sampled taxa. This must
remain \code{TRUE} for templates created from non-intercept workflows, because
the stored model formula still references those predictor columns.}

\item{...}{Reserved for future extensions. Currently ignored.}
}
\value{
A list of class \code{bifrost_simulation_replicate_null} containing the
sampled tree, a single-regime baseline tree, the simulated response matrix,
the combined \code{trait_data} object used for downstream model fitting, and the
generated covariance matrix.
}
\description{
Generate a single no-shift simulation replicate under a uniform Brownian
motion process using the manuscript-style covariance-generation framework
ported into \code{bifrost}. Each replicate draws a fresh positive-definite
covariance matrix using empirical variance and covariance summaries from a
\code{\link[=createSimulationTemplate]{createSimulationTemplate()}} object.
}
\details{
This function is the package-clean analogue of the manuscript's
\code{simulate_traits_BM1()} workflow. The core scientific behavior is preserved:
a new covariance matrix is generated for each replicate from the empirical
variance/covariance summaries in the template, rather than reusing the fitted
covariance matrix directly. For formula-based templates, the empirical fitted
mean structure is added back to the simulated residual process and predictor
columns are preserved unchanged. To keep the stored formula valid for
downstream \code{bifrost} searches, non-intercept templates require
\code{preserve_predictors = TRUE}.
}
\examples{
\dontrun{
set.seed(1)
tr <- ape::rtree(20)
X <- matrix(rnorm(20 * 3), ncol = 3)
rownames(X) <- tr$tip.label
tmpl <- createSimulationTemplate(tr, X, formula = "trait_data ~ 1", method = "LL")

sim_null <- simulateNullDataset(tmpl, tree_tip_count = 15, seed = 2)
str(sim_null$trait_data)
}

}
\seealso{
\code{\link[=createSimulationTemplate]{createSimulationTemplate()}}, \code{\link[=simulateShiftedDataset]{simulateShiftedDataset()}},
\code{\link[=runFalsePositiveSimulationStudy]{runFalsePositiveSimulationStudy()}}
}
