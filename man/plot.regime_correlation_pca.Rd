% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/regime-integration.R
\name{plot.regime_correlation_pca}
\alias{plot.regime_correlation_pca}
\title{Plot Regime Correlation PCA Results}
\usage{
\method{plot}{regime_correlation_pca}(
  x,
  type = c("loadings", "scores", "variance"),
  components = 1:4,
  pc_x = 1L,
  pc_y = 2L,
  palette = NULL,
  main = NULL,
  cluster = c("none", "global", "local"),
  heatmap_engine = c("auto", "base", "ComplexHeatmap"),
  show_dendrogram = TRUE,
  show_legend = TRUE,
  cex.axis = 0.62,
  ...
)
}
\arguments{
\item{x}{A \code{regime_correlation_pca} object.}

\item{type}{Plot type: \code{"loadings"}, \code{"scores"}, or \code{"variance"}.}

\item{components}{Principal components to draw for loading or variance
plots. Supply numeric indices or names such as \code{"PC1"}.}

\item{pc_x, pc_y}{Components for the score scatter plot.}

\item{palette}{Optional color vector for loading heatmaps.}

\item{main}{Optional plot title. For loading plots with multiple
components, supply one title per component.}

\item{cluster}{Loading-heatmap clustering style: \code{"none"} preserves trait
order, \code{"global"} reuses the first selected PC's clustered row/column
order for all panels, and \code{"local"} reclusters each PC panel.}

\item{heatmap_engine}{Loading-heatmap renderer. \code{"auto"} uses
\code{ComplexHeatmap} for clustered heatmaps when that optional package is
installed and otherwise falls back to base graphics.}

\item{show_dendrogram, show_legend}{Logical controls used by the optional
\code{ComplexHeatmap} renderer.}

\item{cex.axis}{Axis label size for loading heatmaps.}

\item{...}{Additional graphical parameters passed to base plotting
functions.}
}
\value{
Invisibly returns \code{x}.
}
\description{
Draw compact PCA views from \code{\link[=regime_correlation_pca]{regime_correlation_pca()}}. The default
\code{"loadings"} view reconstructs upper-triangle loading vectors as symmetric
trait-by-trait heatmaps, matching the Supplementary Figure 4B-style
interpretation of correlation-structure axes.
}
