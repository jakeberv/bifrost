% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/regime-integration.R
\name{fit_regime_covariance_runs}
\alias{fit_regime_covariance_runs}
\title{Fit Post-hoc Regime Covariance Models Across Runs}
\usage{
fit_regime_covariance_runs(
  x,
  trait_data,
  formula = trait_data ~ 1,
  model = "BM",
  min_tips = 2L,
  cores = 1L,
  error = TRUE,
  tree_element = "tree_no_uncertainty_untransformed",
  verbose = FALSE,
  ...
)
}
\arguments{
\item{x}{Named list of \code{bifrost_search} objects, compatible search-like
lists, or SIMMAP-style \code{phylo} trees. Missing or blank names are generated
and names must be unique after normalization.}

\item{trait_data}{Matrix or data frame with unique, non-empty row names
matching the tree tip labels. Column names may be omitted; when present,
they must also be unique and non-empty.}

\item{formula}{Formula used for each regime-specific \code{mvgls()} fit. The
default treats \code{trait_data} as the multivariate response. Formulae may
refer to \code{trait_data} directly, as in \code{trait_data[, 1:5] ~ trait_data[, 6]},
or to columns by name.}

\item{model}{Evolutionary model passed to \code{\link[mvMORPH:mvgls]{mvMORPH::mvgls()}}. The default
\code{"BM"} matches the independent post-hoc covariance workflow.}

\item{min_tips}{Minimum number of tips required before a regime is fitted.
Regimes are fitted when \code{tip_count >= min_tips}; regimes with fewer tips
are returned with status \code{"skipped"}. The default of two tips matches the
Berv et al. post-hoc fitting script.}

\item{cores}{Number of workers. Values greater than one use
\code{\link[future.apply:future_lapply]{future.apply::future_lapply()}} with a temporary multisession plan.}

\item{error}{Logical passed to \code{\link[mvMORPH:mvgls]{mvMORPH::mvgls()}}.}

\item{tree_element}{For search-like list inputs, the element containing the
mapped tree to use for post-hoc refits. Set to \code{NULL} to let
\code{\link[=fit_regime_covariances]{fit_regime_covariances()}} resolve the tree from each run directly.}

\item{verbose}{Logical; if \code{TRUE}, print progress messages by run name.}

\item{...}{Additional arguments passed to \code{\link[mvMORPH:mvgls]{mvMORPH::mvgls()}}.}
}
\value{
A named list of \code{regime_covariances} objects with class
\code{regime_covariance_runs}.
}
\description{
Apply \code{\link[=fit_regime_covariances]{fit_regime_covariances()}} to each search/run in a named list. This is
the package analogue of the manuscript \code{fitPosthocModels()} step: the
top-level run iteration is explicit, while each run can still fit its
regime subtrees in parallel through \code{cores}.
}
