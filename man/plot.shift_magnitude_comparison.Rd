% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/shift-distributions.R
\name{plot.shift_magnitude_comparison}
\alias{plot.shift_magnitude_comparison}
\title{Plot One Shift-Magnitude Comparison}
\usage{
\method{plot}{shift_magnitude_comparison}(
  x,
  scale_by_frequency = TRUE,
  density_args = list(),
  colors = c(increase = "#377eb8", decrease = "#e41a1c"),
  fill_alpha = 0.22,
  lwd = 2,
  xlim = NULL,
  ylim = NULL,
  xlab = NULL,
  ylab = NULL,
  main = NULL,
  annotate = TRUE,
  show_modes = TRUE,
  show_sample_size = TRUE,
  show_test_label = TRUE,
  show_modal_ratio = TRUE,
  legend = TRUE,
  rug = FALSE,
  mar = c(4.5, 4.5, 2.4, 0.8),
  oma = c(0, 0, 0, 0),
  ...
)
}
\arguments{
\item{x}{A single \code{shift_magnitude_comparison} object.}

\item{scale_by_frequency}{Logical; multiply each density by its observed
increase/decrease frequency.}

\item{density_args}{Optional named list of additional arguments passed to
\code{\link[stats:density]{stats::density()}}.}

\item{colors}{Named colors for \code{"increase"} and \code{"decrease"}.}

\item{fill_alpha}{Alpha value for density fills.}

\item{lwd}{Line width for density curves.}

\item{xlim, ylim}{Optional axis limits.}

\item{xlab, ylab, main}{Plot labels.}

\item{annotate}{Logical; draw panel annotations. This is a master switch for
\code{show_sample_size}, \code{show_test_label}, and \code{show_modal_ratio}.}

\item{show_modes}{Logical; draw dashed vertical lines at the KDE modes for
increase and decrease magnitudes.}

\item{show_sample_size}{Logical; include increase/decrease sample sizes in
panel annotations when \code{annotate = TRUE}.}

\item{show_test_label}{Logical; include KS statistics and p-values in panel
annotations when \code{annotate = TRUE} and a KS test is available.}

\item{show_modal_ratio}{Logical; include the back-transformed modal ratio in
panel annotations when \code{annotate = TRUE} and it is finite.}

\item{legend}{Logical; draw an increase/decrease legend.}

\item{rug}{Logical; add rugs for the transformed values.}

\item{mar, oma}{Graphical margins passed to \code{\link[graphics:par]{graphics::par()}}.}

\item{...}{Additional graphical parameters passed to \code{\link[graphics:plot]{graphics::plot()}}.}
}
\value{
Invisibly returns a list with the comparison object, summary tables,
density estimates, and plot settings.
}
\description{
Draw one density panel from a \code{\link[=compare_shift_magnitudes]{compare_shift_magnitudes()}} result. By
default, the method reproduces the Berv et al. (2026) Supplementary Figure 5
scaling: each density integrates to that direction's observed frequency, so
panel height reflects both shift magnitude and relative abundance. Set
\code{scale_by_frequency = FALSE} for conventional unit-area densities.
}
\examples{
transitions <- data.frame(
  rate_change = c("increase", "increase", "increase", "decrease", "decrease", "decrease"),
  rate_delta = c(3, 4, 5, -1, -1.5, -2)
)
comparison <- compare_shift_magnitudes(transitions, ks_reps = 99)
plot(comparison)
}
