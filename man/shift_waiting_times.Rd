% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/shift-distributions.R
\name{shift_waiting_times}
\alias{shift_waiting_times}
\title{Compute Waiting Times Between Regime Shifts}
\usage{
shift_waiting_times(
  x = NULL,
  tree = NULL,
  state_values = NULL,
  scope = c("all", "global", "lineage", "acceleration")
)
}
\arguments{
\item{x}{A BMM \code{bifrost_search} object, compatible list, SIMMAP tree,
\code{shift_transitions} data frame, or \code{NULL} when using \code{tree}.}

\item{tree}{Optional SIMMAP-style \code{phylo} tree for generic input mode, or
the tree corresponding to a user-supplied transition table.}

\item{state_values}{Named numeric state-value vector for generic input mode.}

\item{scope}{Which components to compute. \code{"all"} computes global, lineage,
and acceleration summaries.}
}
\value{
A list of class \code{shift_waiting_times} with \code{transitions}, \code{global},
\code{lineage}, \code{lineage_by_tip}, \code{acceleration}, and \code{summaries} components.
Components outside the requested \code{scope} are returned as \code{NULL}.
}
\description{
Compute chronological whole-tree waiting times, between-shift intervals
along root-to-tip lineages, and acceleration-focused increase-to-increase
waiting times from a mapped SIMMAP tree or from the output of
\code{shift_transitions()}. Lineage intervals exclude the censored
root-to-first-shift and last-shift-to-tip boundaries. Fitted
\code{bifrost_search} inputs are accepted only for multi-regime BMM fits; generic
SIMMAP trees remain supported with \code{tree} and \code{state_values}.
Tied or simultaneous transitions are preserved as zero-length waiting times
in the descriptive tables.
}
\examples{
toy_tree <- ape::read.tree(text = "(((a:1,b:1):1,c:2):1,d:3);")
toy_tree <- phytools::paintSubTree(
  tree = toy_tree,
  node = ape::Ntip(toy_tree) + 1L,
  state = "0"
)
toy_tree <- phytools::paintSubTree(
  tree = toy_tree,
  node = ape::Ntip(toy_tree) + 2L,
  state = "1"
)
toy_tree <- phytools::paintSubTree(
  tree = toy_tree,
  node = ape::Ntip(toy_tree) + 3L,
  state = "2"
)
shift_waiting_times(
  tree = toy_tree,
  state_values = c("0" = 1, "1" = 4, "2" = 2)
)
}
