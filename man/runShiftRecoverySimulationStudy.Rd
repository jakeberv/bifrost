% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulation-framework.R
\name{runShiftRecoverySimulationStudy}
\alias{runShiftRecoverySimulationStudy}
\title{Run a Shift-Recovery Simulation Study}
\usage{
runShiftRecoverySimulationStudy(
  template,
  n_replicates,
  tree_tip_count = NULL,
  simulation_options = list(),
  search_options = list(),
  fuzzy_distance = 2,
  weighted = TRUE,
  num_cores = 1,
  seed = NULL
)
}
\arguments{
\item{template}{A \code{bifrost_simulation_template} returned by
\code{\link[=createSimulationTemplate]{createSimulationTemplate()}}.}

\item{n_replicates}{Integer number of simulation replicates to run.}

\item{tree_tip_count}{Optional integer tip count for random subtree sampling.
If \code{NULL}, each replicate uses the full empirical tree.}

\item{simulation_options}{Named list of additional arguments passed to
\code{\link[=simulateShiftedDataset]{simulateShiftedDataset()}}. For replicated studies,
\code{simulation_options$seed} is not allowed; use the wrapper-level \code{seed}
argument instead.}

\item{search_options}{Named list of arguments passed to
\code{\link[=searchOptimalConfiguration]{searchOptimalConfiguration()}}. These override the default manuscript-style
intercept-only search settings assembled from \code{template}.}

\item{fuzzy_distance}{Integer node distance used for fuzzy matching in
\code{\link[=evaluateShiftRecovery]{evaluateShiftRecovery()}}.}

\item{weighted}{Logical; if \code{TRUE}, compute weighted recovery summaries using
IC weights when available.}

\item{num_cores}{Integer number of workers used across replicate analyses.}

\item{seed}{Optional integer random seed.}
}
\value{
A list of class \code{bifrost_simulation_study} containing the simulated
datasets, raw search results, per-replicate summaries, a study-level
recovery summary, and the output of \code{\link[=evaluateShiftRecovery]{evaluateShiftRecovery()}}.
}
\description{
Repeatedly simulate known-shift datasets from a
\code{\link[=createSimulationTemplate]{createSimulationTemplate()}} object and analyze
each replicate with \code{\link[=searchOptimalConfiguration]{searchOptimalConfiguration()}} to estimate expected shift
recovery performance. The simulation stage supports both the manuscript's
proportional generating-model scenario and its correlation-changing
non-generating robustness scenario.
}
\details{
This function is the package-native port of the manuscript's
\code{run_FN_shift_inference()} workflow. To match the manuscript as closely as
possible, the default search configuration uses \code{uncertaintyweights_par = TRUE}
when \code{weighted = TRUE}, so that shift-recovery summaries can report both
unweighted and IC-weighted metrics.

Regardless of the global calibration model used to build \code{template}, the
downstream search is intentionally restricted to intercept-only formulas.
This keeps the study focused on shift recovery in the simulated response
block rather than re-estimating predictor effects within each replicate.

Failed search replicates are retained in the output and treated as zero-shift
recoveries in the per-replicate summary; the corresponding error messages are
recorded. Reproducibility for replicated studies is controlled by the
wrapper-level \code{seed} argument together with \code{future.seed = TRUE};
per-replicate simulator seeds are intentionally disallowed here.
}
\examples{
\dontrun{
set.seed(1)
tr <- ape::rtree(40)
X <- matrix(rnorm(40 * 3), ncol = 3)
rownames(X) <- tr$tip.label
tmpl <- createSimulationTemplate(tr, X, formula = "trait_data ~ 1", method = "LL")

recovery_study <- runShiftRecoverySimulationStudy(
  tmpl,
  n_replicates = 2,
  tree_tip_count = 25,
  simulation_options = list(
    num_shifts = 2,
    min_shift_tips = 3,
    max_shift_tips = 8,
    scale_mode = "proportional"
  ),
  search_options = list(
    formula = "trait_data ~ 1",
    min_descendant_tips = 3,
    shift_acceptance_threshold = 5,
    num_cores = 1,
    IC = "GIC",
    method = "LL"
  ),
  num_cores = 1,
  seed = 2
)

recovery_study
}

}
\seealso{
\code{\link[=simulateShiftedDataset]{simulateShiftedDataset()}}, \code{\link[=evaluateShiftRecovery]{evaluateShiftRecovery()}},
\code{\link[=runFalsePositiveSimulationStudy]{runFalsePositiveSimulationStudy()}}
}
