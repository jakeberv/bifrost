% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/icTrajectory.R
\name{plot_ic_acceptance_matrix}
\alias{plot_ic_acceptance_matrix}
\title{Plot a Legacy IC Acceptance Matrix}
\usage{
plot_ic_acceptance_matrix(
  matrix_data,
  plot_title = "IC Acceptance Matrix Scatter Plot",
  plot_rate_of_improvement = TRUE,
  rate_limits = c(-400, 150),
  baseline_ic = NULL,
  ...
)
}
\arguments{
\item{matrix_data}{A two-column matrix or data frame. Column 1 must contain
numeric IC scores in evaluation order; column 2 must contain logical values
or 0/1 indicators for accepted proposals.}

\item{plot_title}{Plot title.}

\item{plot_rate_of_improvement}{Logical; if \code{TRUE}, draw the proposal
\code{delta_ic} overlay on a secondary y-axis.}

\item{rate_limits}{Numeric vector of length 2 giving limits for the overlay
axis. Used only when \code{plot_rate_of_improvement = TRUE}.}

\item{baseline_ic}{Optional finite numeric baseline IC. When supplied, this
replaces the first IC value for the plotted baseline row.}

\item{...}{Additional plotting arguments passed to \code{\link[=plot.icTrajectory]{plot.icTrajectory()}}.}
}
\value{
Invisibly returns \code{NULL}. Called for its plotting side effects.
}
\description{
Superseded compatibility wrapper for plotting the historical two-column IC
acceptance matrix. New code should usually call \code{\link[=icTrajectory]{icTrajectory()}} and then
\code{\link[=plot.icTrajectory]{plot.icTrajectory()}} directly.
}
\details{
This wrapper preserves the old \code{plot_ic_acceptance_matrix()} argument names
while delegating the actual drawing to \code{\link[=plot.icTrajectory]{plot.icTrajectory()}}. \code{matrix_data}
must have IC values in the first column and accepted/rejected indicators in
the second column. To match the legacy plotting convention, the first row is
treated as the plotted baseline row; when \code{baseline_ic} is supplied, it
replaces the first IC value for that baseline row.

The historical \code{plot_rate_of_improvement} argument maps to the new
\code{show_delta} plotting mode: \code{TRUE} draws the \code{delta_ic} overlay and \code{FALSE}
suppresses it. \code{rate_limits} is retained as the legacy name for the overlay
y-limits. Its order is ignored for compatibility with the historical wrapper.
}
\seealso{
\code{\link[=icTrajectory]{icTrajectory()}}, \code{\link[=plot.icTrajectory]{plot.icTrajectory()}}
}
