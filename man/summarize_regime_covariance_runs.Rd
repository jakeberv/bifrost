% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/regime-integration.R
\name{summarize_regime_covariance_runs}
\alias{summarize_regime_covariance_runs}
\title{Summarize Post-hoc Regime Covariance Models Across Runs}
\usage{
summarize_regime_covariance_runs(
  x,
  searches = NULL,
  rates = NULL,
  tree = NULL,
  tip_counts = NULL,
  regime_ages = NULL,
  fisher_boundary = c("NA", "error"),
  remove_high_corr = FALSE,
  corr_threshold = 0.95
)
}
\arguments{
\item{x}{Named list of \code{regime_covariances} objects, usually from
\code{\link[=fit_regime_covariance_runs]{fit_regime_covariance_runs()}}. Missing or blank names are generated and
names must be unique after normalization.}

\item{searches}{Optional named list of corresponding \code{bifrost_search}
objects. Run names are matched to \code{x}.}

\item{rates}{Optional named list of regime-rate vectors, or one named rate
vector to reuse for every run.}

\item{tree, tip_counts, regime_ages}{Optional named lists of per-run values,
or a single value to reuse for every run.}

\item{fisher_boundary}{How to handle correlations with \code{abs(r) >= 1}, where
Fisher-Z is undefined. \code{"NA"} returns \code{NA} for those regimes; \code{"error"}
stops with an error.}

\item{remove_high_corr}{Logical; if \code{TRUE}, drop rows whose mean absolute
correlation exceeds \code{corr_threshold}. This reproduces the manuscript
\code{generateVarsCorsList(remove_high_corr = TRUE, corr_threshold = 0.95)}
filtering step.}

\item{corr_threshold}{Correlation threshold used when \code{remove_high_corr} is
\code{TRUE}.}
}
\value{
A named list of summary data frames with class
\code{regime_covariance_run_summaries}.
}
\description{
Apply \code{\link[=summarize_regime_covariances]{summarize_regime_covariances()}} to each element returned by
\code{\link[=fit_regime_covariance_runs]{fit_regime_covariance_runs()}}. This mirrors the manuscript
\code{generateVarsCorsList()} step while keeping rate matching, tip counts, and
high-correlation filtering explicit and reusable.
}
