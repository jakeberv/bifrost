% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/regime-integration.R
\name{fit_regime_covariances}
\alias{fit_regime_covariances}
\title{Fit Independent Post-hoc Regime Covariance Models}
\usage{
fit_regime_covariances(
  x = NULL,
  tree = NULL,
  trait_data,
  formula = trait_data ~ 1,
  model = "BM",
  min_tips = 2L,
  cores = 1L,
  error = TRUE,
  ...
)
}
\arguments{
\item{x}{Optional \code{bifrost_search} object, compatible list with a
\code{tree_no_uncertainty_untransformed} component, or SIMMAP-style \code{phylo}
tree. If \code{tree} is supplied, \code{x} is only used as an optional source of
regime-rate parameters.}

\item{tree}{Optional SIMMAP-style \code{phylo} tree with mapped states, non-empty
regime-state identifiers, and unique, non-empty tip labels. Required when
\code{x} is \code{NULL}.}

\item{trait_data}{Matrix or data frame with unique, non-empty row names
matching the tree tip labels. Column names may be omitted; when present,
they must also be unique and non-empty.}

\item{formula}{Formula used for each regime-specific \code{mvgls()} fit. The
default treats \code{trait_data} as the multivariate response. Formulae may
refer to \code{trait_data} directly, as in \code{trait_data[, 1:5] ~ trait_data[, 6]},
or to columns by name.}

\item{model}{Evolutionary model passed to \code{\link[mvMORPH:mvgls]{mvMORPH::mvgls()}}. The default
\code{"BM"} matches the independent post-hoc covariance workflow.}

\item{min_tips}{Minimum number of tips required before a regime is fitted.
Regimes are fitted when \code{tip_count >= min_tips}; regimes with fewer tips
are returned with status \code{"skipped"}. The default of two tips matches the
Berv et al. post-hoc fitting script.}

\item{cores}{Number of workers. Values greater than one use
\code{\link[future.apply:future_lapply]{future.apply::future_lapply()}} with a temporary multisession plan.}

\item{error}{Logical passed to \code{\link[mvMORPH:mvgls]{mvMORPH::mvgls()}}.}

\item{...}{Additional arguments passed to \code{\link[mvMORPH:mvgls]{mvMORPH::mvgls()}}.}
}
\value{
An object of class \code{regime_covariances}, a list with:
\describe{
\item{fits}{Named list of regime-specific \code{mvgls} fits or \code{NULL}.}
\item{covariances}{Named list of extracted post-hoc covariance matrices or
\code{NULL} for skipped/failed regimes.}
\item{status}{Data frame with regime ID, tip count, regime age, status, and
message.}
\item{rates}{Named regime-rate vector if available from \code{x}; otherwise
\code{NULL}.}
}
}
\description{
Fit separate \code{mvMORPH::mvgls()} Brownian-motion models to the tips assigned
to each contemporary mapped regime. This is a post-hoc workflow: it estimates
independent regime covariance matrices after a mapped-regime analysis has
already been chosen.
}
\details{
The covariance matrices returned here are independent post-hoc estimates.
They are not the same object as \code{search$VCVs} in a \code{bifrost_search} result:
the current \code{bifrost} search model uses scalar transformations of a shared
phenotypic covariance matrix, so \code{search$VCVs} are proportional joint-model
matrices rather than independently refit regime matrices.
Extracted matrices must be symmetric and positive semidefinite, contain
finite entries and strictly positive diagonal variances, and, when named,
have unique matching row and column trait names. Fits that do not satisfy
those requirements are retained with status \code{"failed"} and a diagnostic
message.
}
