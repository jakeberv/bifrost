% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rate-map.R
\name{rateMapControl}
\alias{rateMapControl}
\title{Control Advanced \code{rateMap()} Options}
\usage{
rateMapControl(
  res = 100,
  check = TRUE,
  target = c("first", "mcc"),
  tree_fun = NULL,
  param_fun = NULL,
  na_action = c("error", "omit"),
  rate_flags = rateMapRateFlags(),
  quantile_probs = c(0.025, 0.975),
  highest_density_interval_prob = 0.95,
  future_strategy = c("multisession", "multicore"),
  future_seed = FALSE,
  future_scheduling = 1,
  future_chunk_size = NULL
)
}
\arguments{
\item{res}{Integer resolution of the global depth grid used to subdivide
target-tree branches when \code{summary = "interval"}. Ignored by
\code{summary = "branch"}.}

\item{check}{Logical or character check mode. \code{TRUE} or \code{"full"} verifies
that extracted trees and the target tree match in topology and branch
lengths. \code{"topology"} verifies matching topology/tip labels while allowing
branch lengths to differ. \code{FALSE} or \code{"none"} skips the upfront
\code{\link[ape:all.equal.phylo]{ape::all.equal.phylo()}} check, but target branches still must be
matchable by descendant-tip sets.}

\item{target}{Character target-tree selection when \code{target_tree = NULL} in
\code{\link[=rateMap]{rateMap()}}. \code{"first"} uses the first retained input tree. \code{"mcc"} chooses
the retained input tree with the highest sum of log clade credibilities.}

\item{tree_fun}{Optional function used to extract a mapped tree from each
element of \code{fits}. If \code{NULL}, common \code{bifrost} and mvgls shapes are
auto-detected.}

\item{param_fun}{Optional function used to extract a named numeric vector of
state-specific fitted rates from each element of \code{fits}. If \code{NULL}, common
\code{bifrost} and mvgls shapes are auto-detected.}

\item{na_action}{What to do when a run has invalid parameters. \code{"error"}
stops immediately. \code{"omit"} drops invalid runs before aggregation.}

\item{rate_flags}{A \code{"rateMap_rate_flags"} object from \code{\link[=rateMapRateFlags]{rateMapRateFlags()}},
or a named list of rate-flagging options. Use \code{NULL} to disable rate
diagnostics.}

\item{quantile_probs}{Numeric length-2 vector of quantile probabilities to
report when \code{uncertainty = TRUE}.}

\item{highest_density_interval_prob}{Numeric scalar giving the
highest-density interval mass to report when \code{uncertainty = TRUE}.}

\item{future_strategy}{Future backend used only when \code{workers} is supplied to
\code{\link[=rateMap]{rateMap()}}. Must be one of \code{"multisession"} or \code{"multicore"}.}

\item{future_seed}{Seed control passed to \code{\link[future.apply:future_lapply]{future.apply::future_lapply()}}.}

\item{future_scheduling}{Scheduling control passed to
\code{\link[future.apply:future_lapply]{future.apply::future_lapply()}}.}

\item{future_chunk_size}{Chunk size passed to
\code{\link[future.apply:future_lapply]{future.apply::future_lapply()}}.}
}
\value{
A \code{"rateMap_control"} object for the \code{control} argument of
\code{\link[=rateMap]{rateMap()}}.
}
\description{
Build a control object for less common \code{rateMap()} settings. These options are
useful for interval maps, same-topology tree samples, custom fit object
shapes, invalid-fit handling, and future-based parallel scheduling.
}
\examples{
\dontrun{
# Same-topology tree samples with differing branch lengths:
ctrl <- rateMapControl(check = "topology")
posterior_rates <- rateMap(
  posterior_fit_list,
  target_tree = summary_tree,
  uncertainty = TRUE,
  control = ctrl
)

# Interval maps with a finer depth grid:
interval_rates <- rateMap(
  fits,
  summary = "interval",
  control = rateMapControl(res = 200)
)
}

}
