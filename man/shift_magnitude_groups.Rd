% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/shift-distributions.R
\name{shift_magnitude_groups}
\alias{shift_magnitude_groups}
\title{Define Shift-Magnitude Analysis Groups}
\usage{
shift_magnitude_groups(..., .list = NULL, labels = NULL)
}
\arguments{
\item{...}{Named analysis groups. Use quoted names for labels that are not
syntactic R names, such as \code{"GIC + BIC" = runs}.}

\item{.list}{Optional named list of analysis groups. Use this when groups
are already stored in a list.}

\item{labels}{Optional replacement labels, one per analysis group. Defaults
to names from the supplied groups, falling back to \code{"Analysis 1"},
\code{"Analysis 2"}, and so on when names are absent.}
}
\value{
A named list of class \code{shift_magnitude_groups}.
}
\description{
Wrap named analysis groups so downstream shift-magnitude verbs know to run
once per group rather than pooling every input into one result. Each group
can be a transition table, BMM \code{bifrost_search} object, list of runs to pool,
or a precomputed shift-magnitude object accepted by the downstream verb. When
grouped inputs include precomputed \code{shift_magnitude_comparison} objects,
\code{\link[=compare_shift_magnitudes]{compare_shift_magnitudes()}} requires their stored settings to match the
settings requested for the grouped call.
}
\examples{
transitions <- data.frame(
  rate_change = c("increase", "increase", "decrease", "decrease"),
  rate_delta = c(2, 3, -1, -1.5)
)
groups <- shift_magnitude_groups(
  "GIC + BIC" = list(gic = transitions, bic = transitions),
  GIC = list(gic = transitions)
)
compare_shift_magnitudes(groups, ks_reps = 99)
}
