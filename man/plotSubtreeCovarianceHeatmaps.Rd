% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/focal-disparity-test.R
\name{plotSubtreeCovarianceHeatmaps}
\alias{plotSubtreeCovarianceHeatmaps}
\title{Plot Post-hoc Subtree Covariance and Correlation Heatmaps}
\usage{
plotSubtreeCovarianceHeatmaps(
  tree,
  trait_data,
  focal,
  formula = "trait_data ~ 1",
  model = "BM",
  method = "PL-LOOCV",
  subsets = c("focal", "background"),
  matrix_type = c("covariance", "correlation"),
  min_subtree_tips = 3L,
  draw = TRUE,
  palette = (grDevices::colorRampPalette(c("#3b4cc0", "#f7f7f7", "#b40426")))(101),
  correlation_zlim = c(-1, 1),
  covariance_zlim = NULL,
  cexRow = 0.7,
  cexCol = 0.7,
  margins = c(8, 8),
  ...
)
}
\arguments{
\item{tree}{A rooted phylogeny of class \code{phylo} or coercible with
\code{\link[ape:as.phylo]{ape::as.phylo()}}.}

\item{trait_data}{A numeric matrix or data frame with row names matching tip
labels in \code{tree}.}

\item{focal}{Focal definition. Supply either a character vector of tip
labels, a logical selector of length \code{ape::Ntip(tree)}, or a single
internal node number.}

\item{formula}{Formula passed to \code{\link[mvMORPH:mvgls]{mvMORPH::mvgls()}}. Supports the same named,
response-only, and legacy indexed styles as \code{\link[=testFocalDisparity]{testFocalDisparity()}}.}

\item{model}{Character fit-model code or vector of fit-model codes. Supported
values are \code{"BM"}, \code{"OU"}, and \code{"EB"}.}

\item{method}{Character fitting method passed to \code{\link[mvMORPH:mvgls]{mvMORPH::mvgls()}}.}

\item{subsets}{Which post-hoc subsets to fit. One or both of \code{"focal"} and
\code{"background"}. The background subset is the pruned tree obtained after
removing focal tips.}

\item{matrix_type}{Which matrices to plot. One or both of \code{"covariance"} and
\code{"correlation"}.}

\item{min_subtree_tips}{Minimum number of tips required for each requested
subtree fit.}

\item{draw}{Logical; if \code{TRUE}, draw the requested heatmaps. If multiple
model/subset/matrix combinations are requested, they are drawn in
sequence. On interactive devices, the function asks before advancing to
the next page.}

\item{palette}{Color palette used for all heatmaps.}

\item{correlation_zlim}{Numeric length-2 vector passed as \code{zlim} when
plotting correlation matrices.}

\item{covariance_zlim}{Optional numeric length-2 vector passed as \code{zlim}
when plotting covariance matrices. Defaults to \code{NULL}.}

\item{cexRow}{Character expansion for row labels in the heatmaps.}

\item{cexCol}{Character expansion for column labels in the heatmaps.}

\item{margins}{Margins passed to \code{\link[stats:heatmap]{stats::heatmap()}}.}

\item{...}{Additional arguments passed to \code{\link[mvMORPH:mvgls]{mvMORPH::mvgls()}}.}
}
\value{
Invisibly returns an object of class
\code{bifrost_subtree_covariance_heatmaps}. The object behaves like a nested
named list indexed by model and subset, so existing access patterns such as
\code{x$OU$focal$correlation} continue to work. Each leaf contains the fitted
\code{mvgls} object, the pruned tree, the tip set, the inferred covariance
matrix, the derived correlation matrix, the dendrogram used for plotting,
and a one-row summary table. A model-by-subset summary table and a
focal-vs-background contrast table are stored on the object and returned by
\code{\link[=summary]{summary()}}.
}
\description{
Fit post-hoc \code{mvgls} models on the focal and/or background pruned trees,
extract the inferred residual variance-covariance (\code{sigma}) matrices, convert
them to correlation matrices, and draw clustered heatmaps with dendrograms.
}
\details{
Clustering is performed on the inferred correlation matrix for each fitted
subtree, and the same dendrogram ordering is then reused for both the
covariance and correlation heatmaps from that fit so the two panels remain
directly comparable. Each heatmap is annotated with the fitted subtree size
and a compact summary of covariance magnitude and correlation structure, and
numeric matrix values are overlaid directly on the cells for quick reading.
When multiple covariance heatmaps are drawn in one call and
\code{covariance_zlim = NULL}, a shared symmetric color scale is computed across
all requested covariance matrices.
}
\examples{
\dontrun{
ex <- focalDisparityExampleData(seed = 2)

subtree_mats <- plotSubtreeCovarianceHeatmaps(
  tree = ex$tree,
  trait_data = ex$trait_data,
  focal = ex$focal_tips,
  formula = cbind(y1, y2, y3) ~ size,
  model = c("BM", "OU"),
  subsets = c("focal", "background"),
  matrix_type = c("covariance", "correlation"),
  method = "LL"
)

print(subtree_mats)
summary(subtree_mats)$summary_table

subtree_mats$OU$focal$correlation
}

}
