% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/shift-distributions.R
\name{plot.rate_distribution_fit}
\alias{plot.rate_distribution_fit}
\title{Plot A Fitted Rate Distribution}
\usage{
\method{plot}{rate_distribution_fit}(
  x,
  model = c("gumbel"),
  bootstrap = NULL,
  bootstrap_curves = 120L,
  col = "#b01f2e",
  hist_col = "grey90",
  band_alpha = 0.14,
  curve_alpha = 0.08,
  data_density = TRUE,
  rug = TRUE,
  breaks = "FD",
  main = NULL,
  xlab = NULL,
  ylab = "Density",
  ...
)
}
\arguments{
\item{x}{A \code{rate_distribution_fit} object.}

\item{model}{Distribution model to plot. Currently only \code{"gumbel"} is
supported.}

\item{bootstrap}{\code{NULL} or logical. If \code{NULL}, draw the bootstrap overlay
when precomputed draws for \code{model} are available. If \code{TRUE}, require
precomputed draws from \code{\link[=bootstrap_rate_distribution]{bootstrap_rate_distribution()}}. If \code{FALSE}, draw no
bootstrap overlay.}

\item{bootstrap_curves}{Number of precomputed bootstrap density curves to
draw over the band. Set to \code{0} to draw only the band.}

\item{col}{Color for the fitted density, bootstrap band, and bootstrap
curves.}

\item{hist_col}{Histogram fill color. A vector of colors is passed through
to \code{\link[graphics:hist]{graphics::hist()}} and can color bins individually.}

\item{band_alpha, curve_alpha}{Alpha values for the bootstrap band and
individual bootstrap curves.}

\item{data_density}{Logical; overlay the empirical kernel density.}

\item{rug}{Logical; add a rug for the fitted data values.}

\item{breaks}{Histogram breaks passed to \code{\link[graphics:hist]{graphics::hist()}}.}

\item{main, xlab, ylab}{Plot labels.}

\item{...}{Additional arguments passed to \code{\link[graphics:hist]{graphics::hist()}}.}
}
\value{
Invisibly returns a list with the x grid, fitted density,
bootstrap densities, bootstrap band, and plotted parameter table.
}
\description{
Plot a fitted candidate distribution from \code{\link[=fit_rate_distribution]{fit_rate_distribution()}}. The
current method supports the Gumbel candidate used by Berv et al. (2026). It
can overlay a pointwise bootstrap band and bootstrap density curves when
\code{\link[=bootstrap_rate_distribution]{bootstrap_rate_distribution()}} has already stored bootstrap draws on \code{x}.
}
\examples{
if (requireNamespace("univariateML", quietly = TRUE) &&
    requireNamespace("evd", quietly = TRUE)) {
  fit <- fit_rate_distribution(c(1, 1.4, 1.9, 2.8, 4.1, 6.5), models = "gumbel")
  fit <- bootstrap_rate_distribution(fit, model = "gumbel", reps = 25, seed = 1)
  plot(fit, bootstrap_curves = 5)
}
}
