% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulation-framework.R
\name{runFalsePositiveSimulationStudy}
\alias{runFalsePositiveSimulationStudy}
\title{Run a False-Positive Simulation Study}
\usage{
runFalsePositiveSimulationStudy(
  template,
  n_replicates,
  tree_tip_count = NULL,
  simulation_options = list(),
  search_options = list(),
  num_cores = 1,
  seed = NULL
)
}
\arguments{
\item{template}{A \code{bifrost_simulation_template} returned by
\code{\link[=createSimulationTemplate]{createSimulationTemplate()}}.}

\item{n_replicates}{Integer number of simulation replicates to run.}

\item{tree_tip_count}{Optional integer tip count for random subtree sampling.
If \code{NULL}, each replicate uses the full empirical tree.}

\item{simulation_options}{Named list of additional arguments passed to
\code{\link[=simulateNullDataset]{simulateNullDataset()}}. For replicated studies, \code{simulation_options$seed}
is not allowed; use the wrapper-level \code{seed} argument instead.}

\item{search_options}{Named list of arguments passed to
\code{\link[=searchOptimalConfiguration]{searchOptimalConfiguration()}}. These override the default manuscript-style
intercept-only search settings assembled from \code{template}.}

\item{num_cores}{Integer number of workers used across replicate analyses.}

\item{seed}{Optional integer random seed.}
}
\value{
A list of class \code{bifrost_simulation_study} containing the simulated
datasets, raw search results, a per-replicate summary table, and a compact
study-level summary for the null scenario.
}
\description{
Repeatedly simulate null datasets from a
\code{\link[=createSimulationTemplate]{createSimulationTemplate()}} object and analyze
each replicate with \code{\link[=searchOptimalConfiguration]{searchOptimalConfiguration()}} to estimate the expected
false-positive behavior of a candidate \code{bifrost} search setup.
}
\details{
This function is the package-native port of the manuscript's
\code{run_FP_shift_inference()} workflow. It preserves the main structure of the
original analysis while making the outputs stable and package-friendly:
simulated datasets, search results, a per-replicate summary table, and a
compact study summary are all returned in a single object of class
\code{bifrost_simulation_study}.

Regardless of the global calibration model used to build \code{template}, the
downstream search is intentionally restricted to intercept-only formulas.
This keeps the simulation study focused on branch-shift detection in the
response block, matching the manuscript-style residual-calibration logic.

Failed search replicates are retained in the output and counted as zero
inferred shifts; the corresponding error messages are recorded in the
per-replicate summary. Reproducibility for replicated studies is controlled
by the wrapper-level \code{seed} argument together with \code{future.seed = TRUE};
per-replicate simulator seeds are intentionally disallowed here.
If no replicate yields an evaluable false-positive rate, the study-level mean
and median false-positive summaries are returned as \code{NA}.
}
\examples{
\dontrun{
set.seed(1)
tr <- ape::rtree(30)
X <- matrix(rnorm(30 * 3), ncol = 3)
rownames(X) <- tr$tip.label
tmpl <- createSimulationTemplate(tr, X, formula = "trait_data ~ 1", method = "LL")

fp_study <- runFalsePositiveSimulationStudy(
  tmpl,
  n_replicates = 2,
  tree_tip_count = 20,
  search_options = list(
    formula = "trait_data ~ 1",
    min_descendant_tips = 3,
    shift_acceptance_threshold = 5,
    num_cores = 1,
    IC = "GIC",
    method = "LL"
  ),
  num_cores = 1,
  seed = 2
)

fp_study
}

}
\seealso{
\code{\link[=simulateNullDataset]{simulateNullDataset()}}, \code{\link[=runShiftRecoverySimulationStudy]{runShiftRecoverySimulationStudy()}},
\code{\link[=searchOptimalConfiguration]{searchOptimalConfiguration()}}
}
